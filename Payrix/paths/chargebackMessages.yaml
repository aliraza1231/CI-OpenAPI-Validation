get:
  description: Query a chargebackMessage resource, which represents a message that
    is sent by a Merchant, processor, or bank in relation to a Chargeback.
  parameters:
    - { $ref: ../components/schemas/requestQueryExpandParams.yaml }
    - { $ref: ../components/schemas/requestQueryUnmaskParams.yaml }
    - { $ref: ../components/schemas/requestHeaderSearchParams.yaml }
    - { $ref: ../components/schemas/requestQueryPageNumberParams.yaml }
    - { $ref: ../components/schemas/requestQueryPageLimitParams.yaml }
  responses:
    '200':
      content:
        application/json:
          schema:
            properties:
              response:
                properties:
                  data:
                    description: The data object that the API returns for this request.
                    items:
                      { $ref: ../components/schemas/models/chargebackMessageModel.yaml }
                  details: { $ref: ../components/schemas/details.yaml }
                  errors: { $ref: ../components/schemas/error.yaml }
        application/xml:
          schema:
            properties:
              response:
                properties:
                  data:
                    description: The data object that the API returns for this request.
                    items:
                      { $ref: ../components/schemas/models/chargebackMessageModel.yaml }
                  details: { $ref: ../components/schemas/details.yaml }
                  errors: { $ref: ../components/schemas/error.yaml }
      description: Retrieve Response for query on chargebackMessages resource.
    '400':
      content:
        { $ref: ../components/schemas/error400ResponseContent.yaml }
      description: Error
  summary: Retrieve all Chargeback Messages.
  tags: [ Chargebacks ]
  operationId: getChargebackMessages
post:
  description: Create a chargebackMessage. A chargebackMessage resource represents
    a message that is sent by a Merchant, processor, or bank in relation to a Chargeback.
  requestBody:
    content:
      application/json:
        schema:
          { $ref: ../components/schemas/models/chargebackMessagePostReqBodySchemaModel.yaml }
      application/x-www-form-urlencoded:
        schema:
          { $ref: ../components/schemas/models/chargebackMessagePostReqBodySchemaModel.yaml }
      application/xml:
        schema:
          { $ref: ../components/schemas/models/chargebackMessagePostReqBodySchemaModel.yaml }
      multipart/form-data:
        schema:
          { $ref: ../components/schemas/models/chargebackMessagePostReqBodySchemaModel.yaml }
      text/json:
        schema:
          { $ref: ../components/schemas/models/chargebackMessagePostReqBodySchemaModel.yaml }
      text/xml:
        schema:
          { $ref: ../components/schemas/models/chargebackMessagePostReqBodySchemaModel.yaml }
    description: Create Chargeback Message Request
    required: true
  responses:
    '200':
      content:
        application/json:
          schema:
            properties:
              response:
                properties:
                  data:
                    description: The data object that the API returns for this request.
                    items:
                      { $ref: ../components/schemas/models/chargebackMessageModel.yaml }
                  details: { $ref: ../components/schemas/details.yaml }
                  errors: { $ref: ../components/schemas/error.yaml }
        application/xml:
          schema:
            properties:
              response:
                properties:
                  data:
                    description: The data object that the API returns for this request.
                    items:
                      { $ref: ../components/schemas/models/chargebackMessageModel.yaml }
                  details: { $ref: ../components/schemas/details.yaml }
                  errors: { $ref: ../components/schemas/error.yaml }
      description: Create Chargeback Message Response for creating on chargebackMessages
        resource.
    '400':
      content:
        { $ref: ../components/schemas/error400ResponseContent.yaml }
      description: Error
  summary: Create a Chargeback Message
  tags: [ Chargebacks ]
  operationId: postChargebackMessages
