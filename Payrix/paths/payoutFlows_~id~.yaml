  get:
    description: "Query a payoutFlows resource. \nA payoutFlows resource represents\
      \ a way to set up a Payouts resource automatically for an Entity or Org when it\
      \ is boarded, or when a bank account is associated. \nYou can set up the schedule\
      \ and amount for the Payouts in the payoutFlows resource."
    parameters:
    - description: The ID of this payout flow.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/payoutFlowsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/payoutFlowsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Retrieve the Response for query on Payout Flows resource.
      '400':
        content: {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve a Payout Flow by it's ID.
    tags: [Payouts]
    operationId: getPayoutFlowsId
  put:
    description: Update a payoutFlows resource that represents a way to set up a Payouts
      resource automatically for an Entity or Org when it is boarded, or when a bank
      account is associated, allowing you to set up the schedule and amount for the
      Payouts in the payoutFlows resource.
    parameters:
    - description: The ID of this payout flow.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    requestBody:
      content:
        application/json:
          schema: {$ref: ../components/schemas/models/payoutFlowsRequestModel.yaml}
        application/x-www-form-urlencoded:
          schema: {$ref: ../components/schemas/models/payoutFlowsRequestModel.yaml}
        application/xml:
          schema: {$ref: ../components/schemas/models/payoutFlowsRequestModel.yaml}
        multipart/form-data:
          schema: {$ref: ../components/schemas/models/payoutFlowsRequestModel.yaml}
        text/json:
          schema: {$ref: ../components/schemas/models/payoutFlowsRequestModel.yaml}
        text/xml:
          schema: {$ref: ../components/schemas/models/payoutFlowsRequestModel.yaml}
      description: Update Payout Flow Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/payoutFlowsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/payoutFlowsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for updating a Payout Flow.
      '400':
        content: {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Update a Payout Flow by it's ID.
    tags: [Payouts]
    operationId: putPayoutFlowsId
  delete:
    description: Delete a payoutFlows resource that represents a way to set up a Payouts
      resource automatically for an Entity or Org when it is boarded, or when a bank
      account is associated, allowing you to set up the schedule and amount for the
      Payouts in the payoutFlows resource.
    parameters:
    - description: The ID of this payout flow.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/payoutFlowsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/payoutFlowsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for deleting a Payout Flow.
      '400':
        content: {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Delete a Payout Flow by it's ID.
    tags: [Payouts]
    operationId: deletePayoutFlowsId
