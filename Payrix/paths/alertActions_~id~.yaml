  get:
    description: Query an alertAction; An alertAction resource represents a particular
      instance of an Alert that is sent out through one particular channel, for example,
      SMS, mobile application notification, or email, each alertAction being associated
      with an Alert resource.
    parameters:
    - description: The ID of this resource and The Alert Action ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/alertActionsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/alertActionsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Retrieve Response for query on alertActions resource.
      '400':
        content:
          application/json:
            schema: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema: {$ref: ../components/schemas/error.yaml}
        description: Error
    summary: Retrieve an Alert Action by it's ID.
    tags: [Alert Actions]
    operationId: getAlertActionsId
  put:
    description: Update an alertAction representing a particular instance of an Alert
      that is sent out through one particular channel, for example, SMS, mobile application
      notification, or email, each being associated with an Alert resource.
    parameters:
    - description: The ID of this resource and The Alert Action ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    requestBody:
      content:
        application/json:
          schema: {$ref: ../components/schemas/updateAlertActionsRequestBodySchema.yaml}
        application/x-www-form-urlencoded:
          schema: {$ref: ../components/schemas/updateAlertActionsRequestBodySchema.yaml}
        application/xml:
          schema: {$ref: ../components/schemas/updateAlertActionsRequestBodySchema.yaml}
        multipart/form-data:
          schema: {$ref: ../components/schemas/updateAlertActionsRequestBodySchema.yaml}
        text/json:
          schema: {$ref: ../components/schemas/updateAlertActionsRequestBodySchema.yaml}
        text/xml:
          schema: {$ref: ../components/schemas/updateAlertActionsRequestBodySchema.yaml}
      description: Update Alert Action Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/alertActionsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/alertActionsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for update on alertActions resource Update Alert Action
          Response
      '400':
        content:
          application/json:
            schema: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema: {$ref: ../components/schemas/error.yaml}
        description: Error
    summary: Update an Alert Action by it's ID.
    tags: [Alert Actions]
    operationId: putAlertActionsId
  delete:
    description: Delete an alertAction TBD.
    parameters:
    - description: The ID of this resource and The Alert Action ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/alertActionsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/alertActionsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Delete Alert Action Response for the alertActions resource.
      '400':
        content:
          application/json:
            schema: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema: {$ref: ../components/schemas/error.yaml}
        description: Error
    summary: Delete an Alert Action by it's ID.
    tags: [Alert Actions]
    operationId: deleteAlertActionsId
