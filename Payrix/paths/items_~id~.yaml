  get:
    description: "Query an Item. \nAn Item is a line item that is associated with a\
      \ particular Transaction. It allows you to describe the cost, quantity and other\
      \ details of each line in the Transaction."
    parameters:
    - description: The ID of this resource.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string, description: The Item ID.}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties: {$ref: ../components/schemas/models/itemsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties: {$ref: ../components/schemas/models/itemsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for retrieving an item response.
      '400':
        content: {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve an Item by it's ID.
    tags: [Transactions (Txns)]
    operationId: getItemsId
  put:
    description: "Update an Item. \nAn Item is a line item that is associated with a\
      \ particular Transaction. It allows you to describe the cost, quantity and other\
      \ details of each line in the Transaction."
    parameters:
    - description: The ID of this resource.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string, description: The Item ID.}
      style: simple
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    requestBody:
      content:
        application/json:
          schema:
            properties:
              item: {type: string, description: This is item resource.}
              description:
                type: string
                description: "A description of this Item. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              custom:
                type: string
                description: "A custom identifier for this line Item. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              quantity:
                type: integer
                description: "The quantity of this Item included in the Transaction.\
                  \ \nThis field is specified as an integer."
              price:
                type: integer
                description: "The amount charged for this Item. \nThis field is specified\
                  \ as an integer in cents."
              um:
                type: string
                description: "The unit of measure for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 100 characters long.\
                  \ \nExamples of units of measure may be: each, kilogram, pound, month."
              total:
                type: integer
                description: "The total price for the line item. \nThis field is specified\
                  \ as an integer in cents."
              commodityCode:
                type: string
                description: "The commodity code for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 12 characters long."
              productCode:
                type: string
                description: "The product code for this Item such as: UPC, catalog number,\
                  \ or inventory number. \nThis field is stored as a text string and\
                  \ must be between 0 and 100 characters long."
              discount:
                type: integer
                description: "The discount for the line item. \nThis field is specified\
                  \ as an integer in cents."
              inactive: {$ref: ../components/schemas/inactive.yaml}
              frozen: {$ref: ../components/schemas/frozen.yaml}
            required: [frozen, inactive, item, price, quantity]
            x-redacted: []
        application/x-www-form-urlencoded:
          schema:
            properties:
              item: {}
              description:
                description: "A description of this Item. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              custom:
                description: "A custom identifier for this line Item. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              quantity:
                description: "The quantity of this Item included in the Transaction.\
                  \ \nThis field is specified as an integer."
              price:
                description: "The amount charged for this Item. \nThis field is specified\
                  \ as an integer in cents."
              um:
                description: "The unit of measure for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 100 characters long.\
                  \ \nExamples of units of measure may be: each, kilogram, pound, month."
              total:
                description: "The total price for the line item. \nThis field is specified\
                  \ as an integer in cents."
              commodityCode:
                description: "The commodity code for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 12 characters long."
              productCode:
                description: "The product code for this Item such as: UPC, catalog number,\
                  \ or inventory number. \nThis field is stored as a text string and\
                  \ must be between 0 and 100 characters long."
              discount:
                description: "The discount for the line item. \nThis field is specified\
                  \ as an integer in cents."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [frozen, inactive, item, price, quantity]
            x-redacted: []
        application/xml:
          schema:
            properties:
              item: {}
              description:
                description: "A description of this Item. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              custom:
                description: "A custom identifier for this line Item. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              quantity:
                description: "The quantity of this Item included in the Transaction.\
                  \ \nThis field is specified as an integer."
              price:
                description: "The amount charged for this Item. \nThis field is specified\
                  \ as an integer in cents."
              um:
                description: "The unit of measure for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 100 characters long.\
                  \ \nExamples of units of measure may be: each, kilogram, pound, month."
              total:
                description: "The total price for the line item. \nThis field is specified\
                  \ as an integer in cents."
              commodityCode:
                description: "The commodity code for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 12 characters long."
              productCode:
                description: "The product code for this Item such as: UPC, catalog number,\
                  \ or inventory number. \nThis field is stored as a text string and\
                  \ must be between 0 and 100 characters long."
              discount:
                description: "The discount for the line item. \nThis field is specified\
                  \ as an integer in cents."
              inactive:
                description: Whether this resource is marked as inactive or not.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen or not frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [frozen, inactive, item, price, quantity]
            x-redacted: []
        multipart/form-data:
          schema:
            properties:
              item: {}
              description:
                description: "A description of this Item. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              custom:
                description: "A custom identifier for this line Item. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              quantity:
                description: "The quantity of this Item included in the Transaction.\
                  \ \nThis field is specified as an integer."
              price:
                description: "The amount charged for this Item. \nThis field is specified\
                  \ as an integer in cents."
              um:
                description: "The unit of measure for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 100 characters long.\
                  \ \nExamples of units of measure may be: each, kilogram, pound, month."
              total:
                description: "The total price for the line item. \nThis field is specified\
                  \ as an integer in cents."
              commodityCode:
                description: "The commodity code for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 12 characters long."
              productCode:
                description: "The product code for this Item such as: UPC, catalog number,\
                  \ or inventory number. \nThis field is stored as a text string and\
                  \ must be between 0 and 100 characters long."
              discount:
                description: "The discount for the line item. \nThis field is specified\
                  \ as an integer in cents."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [frozen, inactive, item, price, quantity]
            x-redacted: []
        text/json:
          schema:
            properties:
              item: {}
              description:
                description: "A description of this Item. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              custom:
                description: "A custom identifier for this line Item. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              quantity:
                description: "The quantity of this Item included in the Transaction.\
                  \ \nThis field is specified as an integer."
              price:
                description: "The amount charged for this Item. \nThis field is specified\
                  \ as an integer in cents."
              um:
                description: "The unit of measure for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 100 characters long.\
                  \ \nExamples of units of measure may be: each, kilogram, pound, month."
              total:
                description: "The total price for the line item. \nThis field is specified\
                  \ as an integer in cents."
              commodityCode:
                description: "The commodity code for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 12 characters long."
              productCode:
                description: "The product code for this Item such as: UPC, catalog number,\
                  \ or inventory number. \nThis field is stored as a text string and\
                  \ must be between 0 and 100 characters long."
              discount:
                description: "The discount for the line item. \nThis field is specified\
                  \ as an integer in cents."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [frozen, inactive, item, price, quantity]
            x-redacted: []
        text/xml:
          schema:
            properties:
              item: {}
              description:
                description: "A description of this Item. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              custom:
                description: "A custom identifier for this line Item. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              quantity:
                description: "The quantity of this Item included in the Transaction.\
                  \ \nThis field is specified as an integer."
              price:
                description: "The amount charged for this Item. \nThis field is specified\
                  \ as an integer in cents."
              um:
                description: "The unit of measure for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 100 characters long.\
                  \ \nExamples of units of measure may be: each, kilogram, pound, month."
              total:
                description: "The total price for the line item. \nThis field is specified\
                  \ as an integer in cents."
              commodityCode:
                description: "The commodity code for this Item. \nThis field is stored\
                  \ as a text string and must be between 0 and 12 characters long."
              productCode:
                description: "The product code for this Item such as: UPC, catalog number,\
                  \ or inventory number. \nThis field is stored as a text string and\
                  \ must be between 0 and 100 characters long."
              discount:
                description: "The discount for the line item. \nThis field is specified\
                  \ as an integer in cents."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [frozen, inactive, item, price, quantity]
            x-redacted: []
      description: Update Item Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties: {$ref: ../components/schemas/models/itemsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties: {$ref: ../components/schemas/models/itemsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Successful update of the items
      '400':
        content: {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Update an Item by it's ID.
    tags: [Transactions (Txns)]
    operationId: putItemsId
  delete:
    description: "Delete an Item. \nAn Item is a line item that is associated with a\
      \ particular Transaction. It allows you to describe the cost, quantity and other\
      \ details of each line in the Transaction."
    parameters:
    - description: The ID of this resource.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string, description: The Item ID.}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties: {$ref: ../components/schemas/models/itemsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties: {$ref: ../components/schemas/models/itemsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: The response for deleting an item from the items resource.
      '400':
        content: {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Delete an Item by it's ID.
    tags: [Transactions (Txns)]
    operationId: deleteItemsId
