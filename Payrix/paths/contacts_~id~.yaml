  get:
    description: Query a Contact, which represents an individual who is associated with
      an Entity.
    parameters:
    - description: The ID of this contact.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/contactsResponseModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/contactsResponseModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for retrieve contact.
      '400':
        content: {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve a Contact by their ID.
    tags: [Contacts]
    operationId: getContactsId
  put:
    description: Update a Contact, A Contact resource represents an individual who is
      associated with an Entity.
    parameters:
    - description: The ID of this contact.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    requestBody:
      content:
        application/json:
          schema:
            properties: {$ref: ../components/schemas/models/contactsRequestModel.yaml}
            required: [email, entity, first, frozen, inactive, last]
            x-redacted: []
        application/x-www-form-urlencoded:
          schema:
            properties: {$ref: ../components/schemas/models/contactsRequestModel.yaml}
            required: [email, entity, first, frozen, inactive, last]
            x-redacted: []
        application/xml:
          schema:
            properties: {$ref: ../components/schemas/models/contactsRequestModel.yaml}
            required: [email, entity, first, frozen, inactive, last]
            x-redacted: []
        multipart/form-data:
          schema:
            properties: {$ref: ../components/schemas/models/contactsRequestModel.yaml}
            required: [email, entity, first, frozen, inactive, last]
            x-redacted: []
        text/json:
          schema:
            properties: {$ref: ../components/schemas/models/contactsRequestModel.yaml}
            required: [email, entity, first, frozen, inactive, last]
            x-redacted: []
        text/xml:
          schema:
            properties: {$ref: ../components/schemas/models/contactsRequestModel.yaml}
            required: [email, entity, first, frozen, inactive, last]
            x-redacted: []
      description: Update Contact Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/contactsResponseModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/contactsResponseModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for update on contacts resource Update Contact Response
      '400':
        content: {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve a Contact by their ID.
    tags: [Contacts]
    operationId: putContactsId
  delete:
    description: Delete a Contact. A Contact resource represents an individual who is
      associated with an Entity.
    parameters:
    - description: The ID of this contact.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/contactsResponseModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items: {$ref: ../components/schemas/models/contactsResponseModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for deleting a contact.
      '400':
        content: {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Delete a Contact by their ID.
    tags: [Contacts]
    operationId: deleteContactsId
