  get:
    description: |
      Query a terminalTxnMetadatas resource.
    parameters:
    - description: The Terminal Transaction Metadata ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalTxnsMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalTxnsMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Retrieve Terminal Transaction Metadata Response
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve Terminal Transaction Metadata by it's ID.
    tags: [Transactions (Txns)]
    operationId: getTerminalTxnsMetadatasId
