  get:
    description: Query an Org, which is a collection of Entities that you can associate
      with by creating orgEntity resources.
    parameters:
    - description: The ID of this resource and the Org ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {}
      style: simple
    - description: "Set this path parameter to retrieve nested objects and return them\
        \ as part of the response. \nThe parameter name that you specify determines\
        \ the resources to return. \nFor example, set '?expand[login][]' to return a\
        \ nested login resource."
      explode: true
      in: query
      name: expand
      required: false
      schema: {}
      style: form
    - description: "Set this path parameter to retrieve unmasked details in the response.\
        \ \nThe fields specified will be returned in full. Only works for single-record\
        \ requests."
      explode: true
      in: query
      name: unmask
      required: false
      schema: {}
      style: form
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The Login that owns this Org.}
                          name:
                            description: "The name of this Org. \nThis field is stored\
                              \ as a text string and must be between 0 and 100 characters\
                              \ long."
                          description:
                            description: "A description of this Org. \nThis field is\
                              \ stored as a text string and must be between 0 and 100\
                              \ characters long."
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
                data:
                  description: The data object that the API returns for this request.
                  items:
                    properties:
                      id: {description: The ID of this resource.}
                      created: {description: The date and time at which this resource
                          was created., format: date-time}
                      modified: {description: The date and time at which this resource
                          was modified., format: date-time}
                      creator: {description: The identifier of the Login that created
                          this resource.}
                      modifier: {description: The identifier of the Login that last
                          modified this resource.}
                      login: {description: The Login that owns this Org.}
                      name: {description: The name of this Org. This field is stored
                          as a text string and must be between 0 and 100 characters
                          long.}
                      description: {description: A description of this Org. This field
                          is stored as a text string and must be between 0 and 100 characters
                          long.}
                details:
                  description: An object describing the response, including the request
                    ID and pagination indicators.
                  properties:
                    requestId:
                      description: "A unique identifier set by the API client, that\
                        \ is echoed by the response. \nThe request ID can be useful\
                        \ in troubleshooting and monitoring."
                    page:
                      description: "Where the response lists multiple resources, the\
                        \ API splits the response into several 'pages'. \nThis object\
                        \ indicates the current and last available pages in the list."
                      properties:
                        current: {description: The current page in the paginated resource
                            list.}
                        last: {description: The last available page in the paginated
                            resource list.}
                errors:
                  description: An array of zero or more errors that occurred when the
                    API processed the request.
                  items:
                    properties:
                      code: {description: The error code associated with a particular
                          error.}
                      severity:
                        description: "The severity level of a particular error. \nPossible\
                          \ values are '0' (informational), '1' (warning), '2' (error)\
                          \ and '4' (failure)."
                      message: {description: The message associated with a particular
                          error.}
                      errorCode: {description: An identifying string code for this error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The Login that owns this Org.}
                          name:
                            description: "The name of this Org. \nThis field is stored\
                              \ as a text string and must be between 0 and 100 characters\
                              \ long."
                          description:
                            description: "A description of this Org. \nThis field is\
                              \ stored as a text string and must be between 0 and 100\
                              \ characters long."
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
                data:
                  description: The data object that the API returns for this request.
                  items:
                    properties:
                      id: {description: The ID of this resource.}
                      created: {description: The date and time at which this resource
                          was created., format: date-time}
                      modified: {description: The date and time at which this resource
                          was modified., format: date-time}
                      creator: {description: The identifier of the Login that created
                          this resource.}
                      modifier: {description: The identifier of the Login that last
                          modified this resource.}
                      login: {description: The Login that owns this Org.}
                      name: {description: The name of this Org. This field is stored
                          as a text string and must be between 0 and 100 characters
                          long.}
                      description: {description: A description of this Org. This field
                          is stored as a text string and must be between 0 and 100 characters
                          long.}
                details:
                  description: An object describing the response, including the request
                    ID and pagination indicators.
                  properties:
                    requestId:
                      description: "A unique identifier set by the API client, that\
                        \ is echoed by the response. \nThe request ID can be useful\
                        \ in troubleshooting and monitoring."
                    page:
                      description: "Where the response lists multiple resources, the\
                        \ API splits the response into several 'pages'. \nThis object\
                        \ indicates the current and last available pages in the list."
                      properties:
                        current: {description: The current page in the paginated resource
                            list.}
                        last: {description: The last available page in the paginated
                            resource list.}
                errors:
                  description: An array of zero or more errors that occurred when the
                    API processed the request.
                  items:
                    properties:
                      code: {description: The error code associated with a particular
                          error.}
                      severity:
                        description: "The severity level of a particular error. \nPossible\
                          \ values are '0' (informational), '1' (warning), '2' (error)\
                          \ and '4' (failure)."
                      message: {description: The message associated with a particular
                          error.}
                      errorCode: {description: An identifying string code for this error.}
        description: Retrieve an Org Response for query on orgs resource.
      '400':
        content:
          application/json:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
          application/xml:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
        description: Error
    summary: Retrieve an Org by it's ID.
    tags: [Orgs]
    operationId: getOrgsId
  put:
    description: Update an Org, which is a collection of Entities that you can associate
      with other entities by creating orgEntity resources.
    parameters:
    - description: The ID of this resource and the Org ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {}
      style: simple
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    requestBody:
      content:
        application/json:
          schema:
            properties:
              login: {description: The Login that owns this Org.}
              name: {description: The name of this Org. This field is stored as a text
                  string and must be between 0 and 100 characters long.}
              description: {description: A description of this Org. This field is stored
                  as a text string and must be between 0 and 100 characters long.}
            required: [login]
            x-redacted: []
        application/x-www-form-urlencoded:
          schema:
            properties:
              login: {description: The Login that owns this Org.}
              name:
                description: "The name of this Org. \nThis field is stored as a text\
                  \ string and must be between 0 and 100 characters long."
              description:
                description: "A description of this Org. \nThis field is stored as a\
                  \ text string and must be between 0 and 100 characters long."
            required: [login]
            x-redacted: []
        application/xml:
          schema:
            properties:
              login: {description: The Login that owns this Org.}
              name: {description: The name of this Org. This field is stored as a text
                  string and must be between 0 and 100 characters long.}
              description: {description: A description of this Org. This field is stored
                  as a text string and must be between 0 and 100 characters long.}
            required: [login]
            x-redacted: []
        multipart/form-data:
          schema:
            properties:
              login: {description: The Login that owns this Org.}
              name:
                description: "The name of this Org. \nThis field is stored as a text\
                  \ string and must be between 0 and 100 characters long."
              description:
                description: "A description of this Org. \nThis field is stored as a\
                  \ text string and must be between 0 and 100 characters long."
            required: [login]
            x-redacted: []
        text/json:
          schema:
            properties:
              login: {description: The Login that owns this Org.}
              name:
                description: "The name of this Org. \nThis field is stored as a text\
                  \ string and must be between 0 and 100 characters long."
              description:
                description: "A description of this Org. \nThis field is stored as a\
                  \ text string and must be between 0 and 100 characters long."
            required: [login]
            x-redacted: []
        text/xml:
          schema:
            properties:
              login: {description: The Login that owns this Org.}
              name:
                description: "The name of this Org. \nThis field is stored as a text\
                  \ string and must be between 0 and 100 characters long."
              description:
                description: "A description of this Org. \nThis field is stored as a\
                  \ text string and must be between 0 and 100 characters long."
            required: [login]
            x-redacted: []
      description: Update an Org Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The Login that owns this Org.}
                          name:
                            description: "The name of this Org. \nThis field is stored\
                              \ as a text string and must be between 0 and 100 characters\
                              \ long."
                          description:
                            description: "A description of this Org. \nThis field is\
                              \ stored as a text string and must be between 0 and 100\
                              \ characters long."
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
                data:
                  description: The data object that the API returns for this request.
                  items:
                    properties:
                      id: {description: The ID of this resource.}
                      created: {description: The date and time at which this resource
                          was created., format: date-time}
                      modified: {description: The date and time at which this resource
                          was modified., format: date-time}
                      creator: {description: The identifier of the Login that created
                          this resource.}
                      modifier: {description: The identifier of the Login that last
                          modified this resource.}
                      login: {description: The Login that owns this Org.}
                      name: {description: The name of this Org. This field is stored
                          as a text string and must be between 0 and 100 characters
                          long.}
                      description: {description: A description of this Org. This field
                          is stored as a text string and must be between 0 and 100 characters
                          long.}
                details:
                  description: An object describing the response, including the request
                    ID and pagination indicators.
                  properties:
                    requestId:
                      description: "A unique identifier set by the API client, that\
                        \ is echoed by the response. \nThe request ID can be useful\
                        \ in troubleshooting and monitoring."
                    page:
                      description: "Where the response lists multiple resources, the\
                        \ API splits the response into several 'pages'. \nThis object\
                        \ indicates the current and last available pages in the list."
                      properties:
                        current: {description: The current page in the paginated resource
                            list.}
                        last: {description: The last available page in the paginated
                            resource list.}
                errors:
                  description: An array of zero or more errors that occurred when the
                    API processed the request.
                  items:
                    properties:
                      code: {description: The error code associated with a particular
                          error.}
                      severity:
                        description: "The severity level of a particular error. \nPossible\
                          \ values are '0' (informational), '1' (warning), '2' (error)\
                          \ and '4' (failure)."
                      message: {description: The message associated with a particular
                          error.}
                      errorCode: {description: An identifying string code for this error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The Login that owns this Org.}
                          name:
                            description: "The name of this Org. \nThis field is stored\
                              \ as a text string and must be between 0 and 100 characters\
                              \ long."
                          description:
                            description: "A description of this Org. \nThis field is\
                              \ stored as a text string and must be between 0 and 100\
                              \ characters long."
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
                data:
                  description: The data object that the API returns for this request.
                  items:
                    properties:
                      id: {description: The ID of this resource.}
                      created: {description: The date and time at which this resource
                          was created., format: date-time}
                      modified: {description: The date and time at which this resource
                          was modified., format: date-time}
                      creator: {description: The identifier of the Login that created
                          this resource.}
                      modifier: {description: The identifier of the Login that last
                          modified this resource.}
                      login: {description: The Login that owns this Org.}
                      name: {description: The name of this Org. This field is stored
                          as a text string and must be between 0 and 100 characters
                          long.}
                      description: {description: A description of this Org. This field
                          is stored as a text string and must be between 0 and 100 characters
                          long.}
                details:
                  description: An object describing the response, including the request
                    ID and pagination indicators.
                  properties:
                    requestId:
                      description: "A unique identifier set by the API client, that\
                        \ is echoed by the response. \nThe request ID can be useful\
                        \ in troubleshooting and monitoring."
                    page:
                      description: "Where the response lists multiple resources, the\
                        \ API splits the response into several 'pages'. \nThis object\
                        \ indicates the current and last available pages in the list."
                      properties:
                        current: {description: The current page in the paginated resource
                            list.}
                        last: {description: The last available page in the paginated
                            resource list.}
                errors:
                  description: An array of zero or more errors that occurred when the
                    API processed the request.
                  items:
                    properties:
                      code: {description: The error code associated with a particular
                          error.}
                      severity:
                        description: "The severity level of a particular error. \nPossible\
                          \ values are '0' (informational), '1' (warning), '2' (error)\
                          \ and '4' (failure)."
                      message: {description: The message associated with a particular
                          error.}
                      errorCode: {description: An identifying string code for this error.}
        description: 'Response for update on orgs resource: Update an Org Response'
      '400':
        content:
          application/json:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
          application/xml:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
        description: Error
    summary: Update an Org by it's ID.
    tags: [Orgs]
    x-examples:
    - request: {id: p1_org_5a1ef5e55657549826fa89a, name: Default Group}
      response: {id: p1_org_5a1ef5e55657549826fa89a, created: '2017-11-28T01:02:57Z',
        modified: '2017-11-29T01:02:57Z', creator: p1_log_5a1ef5e55653ed720159d53, modifier: p1_log_5a1ef5e55653ed720159d53,
        login: p1_log_5a1ef5e55653ed720159d53, name: Default Group, description: ''}
    operationId: putOrgsId
  delete:
    description: Delete an Organization, which is a collection of Entities that you
      can associate with it by creating organization-entity resources.
    parameters:
    - description: The ID of this resource and The Org ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {}
      style: simple
    - description: "Set this path parameter to retrieve nested objects and return them\
        \ as part of the response. \nThe parameter name that you specify determines\
        \ the resources to return. \nFor example, set '?expand[login][]' to return a\
        \ nested login resource."
      explode: true
      in: query
      name: expand
      required: false
      schema: {}
      style: form
    - description: "Set this path parameter to retrieve unmasked details in the response.\
        \ \nThe fields specified will be returned in full. Only works for single-record\
        \ requests."
      explode: true
      in: query
      name: unmask
      required: false
      schema: {}
      style: form
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The Login that owns this Org.}
                          name:
                            description: "The name of this Org. \nThis field is stored\
                              \ as a text string and must be between 0 and 100 characters\
                              \ long."
                          description:
                            description: "A description of this Org. \nThis field is\
                              \ stored as a text string and must be between 0 and 100\
                              \ characters long."
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
                data:
                  description: The data object that the API returns for this request.
                  items:
                    properties:
                      id: {description: The ID of this resource.}
                      created: {description: The date and time at which this resource
                          was created., format: date-time}
                      modified: {description: The date and time at which this resource
                          was modified., format: date-time}
                      creator: {description: The identifier of the Login that created
                          this resource.}
                      modifier: {description: The identifier of the Login that last
                          modified this resource.}
                      login: {description: The Login that owns this Org.}
                      name: {description: The name of this Org. This field is stored
                          as a text string and must be between 0 and 100 characters
                          long.}
                      description: {description: A description of this Org. This field
                          is stored as a text string and must be between 0 and 100 characters
                          long.}
                details:
                  description: An object describing the response, including the request
                    ID and pagination indicators.
                  properties:
                    requestId:
                      description: "A unique identifier set by the API client, that\
                        \ is echoed by the response. \nThe request ID can be useful\
                        \ in troubleshooting and monitoring."
                    page:
                      description: "Where the response lists multiple resources, the\
                        \ API splits the response into several 'pages'. \nThis object\
                        \ indicates the current and last available pages in the list."
                      properties:
                        current: {description: The current page in the paginated resource
                            list.}
                        last: {description: The last available page in the paginated
                            resource list.}
                errors:
                  description: An array of zero or more errors that occurred when the
                    API processed the request.
                  items:
                    properties:
                      code: {description: The error code associated with a particular
                          error.}
                      severity:
                        description: "The severity level of a particular error. \nPossible\
                          \ values are '0' (informational), '1' (warning), '2' (error)\
                          \ and '4' (failure)."
                      message: {description: The message associated with a particular
                          error.}
                      errorCode: {description: An identifying string code for this error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The Login that owns this Org.}
                          name:
                            description: "The name of this Org. \nThis field is stored\
                              \ as a text string and must be between 0 and 100 characters\
                              \ long."
                          description:
                            description: "A description of this Org. \nThis field is\
                              \ stored as a text string and must be between 0 and 100\
                              \ characters long."
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
                data:
                  description: The data object that the API returns for this request.
                  items:
                    properties:
                      id: {description: The ID of this resource.}
                      created: {description: The date and time at which this resource
                          was created., format: date-time}
                      modified: {description: The date and time at which this resource
                          was modified., format: date-time}
                      creator: {description: The identifier of the Login that created
                          this resource.}
                      modifier: {description: The identifier of the Login that last
                          modified this resource.}
                      login: {description: The Login that owns this Org.}
                      name: {description: The name of this Org. This field is stored
                          as a text string and must be between 0 and 100 characters
                          long.}
                      description: {description: A description of this Org. This field
                          is stored as a text string and must be between 0 and 100 characters
                          long.}
                details:
                  description: An object describing the response, including the request
                    ID and pagination indicators.
                  properties:
                    requestId:
                      description: "A unique identifier set by the API client, that\
                        \ is echoed by the response. \nThe request ID can be useful\
                        \ in troubleshooting and monitoring."
                    page:
                      description: "Where the response lists multiple resources, the\
                        \ API splits the response into several 'pages'. \nThis object\
                        \ indicates the current and last available pages in the list."
                      properties:
                        current: {description: The current page in the paginated resource
                            list.}
                        last: {description: The last available page in the paginated
                            resource list.}
                errors:
                  description: An array of zero or more errors that occurred when the
                    API processed the request.
                  items:
                    properties:
                      code: {description: The error code associated with a particular
                          error.}
                      severity:
                        description: "The severity level of a particular error. \nPossible\
                          \ values are '0' (informational), '1' (warning), '2' (error)\
                          \ and '4' (failure)."
                      message: {description: The message associated with a particular
                          error.}
                      errorCode: {description: An identifying string code for this error.}
        description: Response for deleting an org.
      '400':
        content:
          application/json:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
          application/xml:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
        description: Error
    summary: Delete an Org by it's ID.
    tags: [Orgs]
    operationId: deleteOrgsId
