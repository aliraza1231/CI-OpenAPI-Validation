  get:
    description: |
      Query a terminalTxnMetadatas resource.
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalTxnsMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalTxnsMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Retrieve Terminal Transaction Metadata Response
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve all Terminal Transaction Metadata.
    tags: [Transactions (Txns)]
    operationId: getTerminalTxnsMetadatas
  post:
    description: |
      Create a terminalTxnMetadatas resource.
    requestBody:
      content:
        application/json:
          schema:
            {$ref: ../components/schemas/models/terminalTxnsMetadatasPostReqBodySchemaModel.yaml}
        application/xml:
          schema:
            {$ref: ../components/schemas/models/terminalTxnsMetadatasPostReqBodySchemaModel.yaml}
      description: Create Terminal Transaction Metadata Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalTxnsMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalTxnsMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Create Terminal Transaction Metadata Response
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Create a new Terminal Transaction Metadata
    tags: [Transactions (Txns)]
    operationId: postTerminalTxnsMetadatas
