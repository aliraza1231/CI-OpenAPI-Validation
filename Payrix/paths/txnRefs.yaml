  get:
    description: "Query a txnRefs resource. \nA txnRefs resource represents a reference\
      \ code issued by the Processor in relation to a particular Transaction."
    parameters:
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    - {$ref: ../components/schemas/requestQueryPageNumberParams.yaml}
    - {$ref: ../components/schemas/requestQueryPageLimitParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/txnRefsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/txnRefsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for query on txnRefs resource
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve all transaction references (txnRefs).
    tags: [Transactions (Txns)]
    operationId: getTxnRefs
  post:
    description: "Create a txnRefs resource. \nA txnRefs resource represents a reference\
      \ code issued by the Processor in relation to a particular Transaction."
    requestBody:
      content:
        application/json:
          schema:
            {$ref: ../components/schemas/models/txnRefsPostReqBodySchemaModel.yaml}
        application/x-www-form-urlencoded:
          schema:
            {$ref: ../components/schemas/models/txnRefsPostReqBodySchemaModel.yaml}
        application/xml:
          schema:
            {$ref: ../components/schemas/models/txnRefsPostReqBodySchemaModel.yaml}
        multipart/form-data:
          schema:
            {$ref: ../components/schemas/models/txnRefsPostReqBodySchemaModel.yaml}
        text/json:
          schema:
            {$ref: ../components/schemas/models/txnRefsPostReqBodySchemaModel.yaml}
        text/xml:
          schema:
            {$ref: ../components/schemas/models/txnRefsPostReqBodySchemaModel.yaml}
      description: ''
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/txnRefsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/txnRefsPostReqBodySchemaModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for create on txnRefs resource
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Create a transaction reference (txnRefs).
    tags: [Transactions (Txns)]
    operationId: postTxnRefs
