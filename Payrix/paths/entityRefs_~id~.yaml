  get:
    description: Query an EntityRefs resource that represents a reference code issued
      by the Processor that represents a particular Entity.
    parameters:
    - description: The ID of this entity reference.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/entityRefModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/entityRefModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: The response for retrieving an Entity Reference.
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve an Entity Reference by it's ID.
    tags: [EntityRefs]
    operationId: getEntityRefsId
  put:
    description: Update an entityRefs resource, which represents a reference code issued
      by the Processor that represents a particular Entity.
    parameters:
    - description: The ID of this entity reference.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    requestBody:
      content:
        application/json:
          schema:
            properties: {$ref: ../components/schemas/models/entityRefsPutReqBodySchemaModel.yaml}
            required: [default, frozen, inactive, ref, staging]
            x-redacted: []
        application/x-www-form-urlencoded:
          schema:
            properties: {$ref: ../components/schemas/models/entityRefsPutReqBodySchemaModel.yaml}
            required: [default, frozen, inactive, ref, staging]
            x-redacted: []
        application/xml:
          schema:
            properties: {$ref: ../components/schemas/models/entityRefsPutReqBodySchemaModel.yaml}
            required: [default, frozen, inactive, ref, staging]
            x-redacted: []
        multipart/form-data:
          schema:
            properties: {$ref: ../components/schemas/models/entityRefsPutReqBodySchemaModel.yaml}
            required: [default, frozen, inactive, ref, staging]
            x-redacted: []
        text/json:
          schema:
            properties: {$ref: ../components/schemas/models/entityRefsPutReqBodySchemaModel.yaml}
            required: [default, frozen, inactive, ref, staging]
            x-redacted: []
        text/xml:
          schema:
            properties: {$ref: ../components/schemas/models/entityRefsPutReqBodySchemaModel.yaml}
            required: [default, frozen, inactive, ref, staging]
            x-redacted: []
      description: Update an Entity Reference Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/entityRefModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/entityRefModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: The response for updating an entity reference.
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Update an Entity Reference by it's ID.
    tags: [EntityRefs]
    operationId: putEntityRefsId
  delete:
    description: Delete an entityRefs resource, which represents a reference code issued
      by the Processor that represents a particular Entity.
    parameters:
    - description: The ID of this entity reference.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/entityRefModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/entityRefModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: |-
          The response for deleting an entity reference.

          Details:

          * None
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Delete an Entity Reference by it's ID.
    tags: [EntityRefs]
    operationId: deleteEntityRefsId
