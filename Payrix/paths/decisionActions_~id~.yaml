  get:
    description: Query a Decision Action, which is a piece of conditional logic that
      makes a VerificationResult change the application and the decision action in certain
      circumstances.
    parameters:
    - description: The ID of this resource and The Decision Action ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {}
      style: simple
    - description: "Set this path parameter to retrieve nested objects and return them\
        \ as part of the response. \nThe parameter name that you specify determines\
        \ the resources to return. \nFor example, set '?expand[login][]' to return a\
        \ nested login resource."
      explode: true
      in: query
      name: expand
      required: false
      schema: {}
      style: form
    - description: "Set this path parameter to retrieve unmasked details in the response.\
        \ \nThe fields specified will be returned in full. Only works for single-record\
        \ requests."
      explode: true
      in: query
      name: unmask
      required: false
      schema: {}
      style: form
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          decision: {description: The identifier of the Decision to
                              which this resource applies}
                          action:
                            description: The action to take when this check fails.
                            enum: ['1', '3', '4', '5', '8']
                            x-enum-descriptions: ['Return an error and decline. For
                                transactions, this results in a permanent decline. For
                                merchants, they may still be approved and the decline
                                removed later on.', 'For txn decision: Hold. Hold the
                                Transaction. It will not be captured until it is manually
                                released.', 'For txn decision: Reserve. Reserve the
                                Transaction. The funds for the transaction will not
                                be released until the Transaction is manually reviewed.',
                              This decision results in the merchant's current activity
                                (either transaction or payout) being declined., '']
                          application:
                            description: Where the action defined by this DecisionAction
                              should apply
                            enum: [account, txn, entity]
                            x-enum-descriptions: ['', Applies the action to the transaction,
                              Applies the action on the entity]
                          scoreType:
                            description: The type of score result on the related Decision
                            enum: [low, high, none]
                            x-enum-descriptions: ['', '', '']
                          type:
                            description: The type of DecisionAction
                            enum: [equal, notEqual, contains, greater, less]
                            x-enum-descriptions: ['', '', '', '', '']
                          field: {description: The result field to check}
                          score: {description: The score to check.}
                          data: {}
                          message: {}
                          code: {}
                          grouping: {description: 'A name for a group of decisionActions
                              to be applied in conjunction when evaluating this DecisionAction;
                              when grouping is used, the DecisionActions will not apply
                              unless they all match.'}
                          inactive:
                            description: "Whether this resource is marked as inactive.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Active** \n\n- `1` - **Inactive**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: "Whether this resource is marked as frozen.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Not Frozen** \n\n- `1` - **Frozen**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          decision: {description: The identifier of the Decision to
                              which this resource applies}
                          action:
                            description: The action to take when this check fails.
                            enum: ['1', '3', '4', '5', '8']
                            x-enum-descriptions: ['Return an error and decline. For
                                transactions, this results in a permanent decline. For
                                merchants, they may still be approved and the decline
                                removed later on.', 'For txn decision: Hold. Hold the
                                Transaction. It will not be captured until it is manually
                                released.', 'For txn decision: Reserve. Reserve the
                                Transaction. The funds for the transaction will not
                                be released until the Transaction is manually reviewed.',
                              This decision results in the merchant's current activity
                                (either transaction or payout) being declined., '']
                          application:
                            description: Where the action defined by this DecisionAction
                              should apply
                            enum: [account, txn, entity]
                            x-enum-descriptions: ['', Applies the action to the transaction,
                              Applies the action on the entity]
                          scoreType:
                            description: The type of score result on the related Decision
                            enum: [low, high, none]
                            x-enum-descriptions: ['', '', '']
                          type:
                            description: The type of DecisionAction
                            enum: [equal, notEqual, contains, greater, less]
                            x-enum-descriptions: ['', '', '', '', '']
                          field: {description: The result field to check}
                          score: {description: The score to check.}
                          data: {}
                          message: {}
                          code: {}
                          grouping: {description: 'A name for a group of decisionActions
                              to be applied in conjunction when evaluating this DecisionAction;
                              when grouping is used, the DecisionActions will not apply
                              unless they all match.'}
                          inactive:
                            description: "Whether this resource is marked as inactive.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Active** \n\n- `1` - **Inactive**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: "Whether this resource is marked as frozen.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Not Frozen** \n\n- `1` - **Frozen**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: Retrieve the Response for the query on the Decision Actions resource.
      '400':
        content:
          application/json:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
          application/xml:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
        description: Error
    summary: Retrieve Decision Action by it's ID.
    tags: [Decision Actions]
    operationId: getDecisionActionsId
  put:
    description: Update a Decision Action, which is a piece of conditional logic that
      makes a VerificationResult change the application and the decision action in certain
      circumstances.
    parameters:
    - description: The ID of this resource and The Decision Action ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {}
      style: simple
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    requestBody:
      content:
        application/json:
          schema:
            properties:
              decision: {description: The identifier of the Decision to which this resource
                  applies}
              action:
                description: The action to take when this check fails.
                enum: ['1', '3', '4', '5', '8']
                x-enum-descriptions: ['Return an error and decline. For transactions,
                    this results in a permanent decline. For merchants, they may still
                    be approved and the decline removed later on.', 'For txn decision:
                    Hold. Hold the Transaction. It will not be captured until it is
                    manually released.', 'For txn decision: Reserve. Reserve the Transaction.
                    The funds for the transaction will not be released until the Transaction
                    is manually reviewed.', This decision results in the merchant's
                    current activity (either transaction or payout) being declined.,
                  '']
              application:
                description: Where the action defined by this DecisionAction should
                  apply
                enum: [account, txn, entity]
                x-enum-descriptions: ['', Applies the action to the transaction, Applies
                    the action on the entity]
              scoreType:
                description: The type of score result on the related Decision
                enum: [low, high, none]
                x-enum-descriptions: ['', '', '']
              type:
                description: The type of DecisionAction
                enum: [equal, notEqual, contains, greater, less]
                x-enum-descriptions: ['', '', '', '', '']
              field: {description: The result field to check}
              data: {}
              message: {}
              code: {}
              score: {description: The score to check.}
              grouping: {description: 'A name for a group of decisionActions to be applied
                  in conjunction when evaluating this DecisionAction; when grouping
                  is used, the DecisionActions will not apply unless they all match.'}
              inactive:
                description: "Whether this resource is marked as inactive.\n<details>\n\
                  <summary>Valid Values</summary>\n\n- `0` - **Active** \n\n- `1` -\
                  \ **Inactive**\n</details>\n"
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: "Whether this resource is marked as frozen.\n<details>\n\
                  <summary>Valid Values</summary>\n\n- `0` - **Not Frozen** \n\n- `1`\
                  \ - **Frozen**\n</details>\n"
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [decision, frozen, inactive]
            x-redacted: []
        application/x-www-form-urlencoded:
          schema:
            properties:
              decision: {description: The identifier of the Decision to which this resource
                  applies}
              action:
                description: The action to take when this check fails.
                enum: ['1', '3', '4', '5', '8']
                x-enum-descriptions: ['Return an error and decline. For transactions,
                    this results in a permanent decline. For merchants, they may still
                    be approved and the decline removed later on.', 'For txn decision:
                    Hold. Hold the Transaction. It will not be captured until it is
                    manually released.', 'For txn decision: Reserve. Reserve the Transaction.
                    The funds for the transaction will not be released until the Transaction
                    is manually reviewed.', This decision results in the merchant's
                    current activity (either transaction or payout) being declined.,
                  '']
              application:
                description: Where the action defined by this DecisionAction should
                  apply
                enum: [account, txn, entity]
                x-enum-descriptions: ['', Applies the action to the transaction, Applies
                    the action on the entity]
              scoreType:
                description: The type of score result on the related Decision
                enum: [low, high, none]
                x-enum-descriptions: ['', '', '']
              type:
                description: The type of DecisionAction
                enum: [equal, notEqual, contains, greater, less]
                x-enum-descriptions: ['', '', '', '', '']
              field: {description: The result field to check}
              data: {}
              message: {}
              code: {}
              score: {description: The score to check.}
              grouping:
                description: "A name for a group of decisionActions to be applied in\
                  \ conjunction when evaluating this DecisionAction. \nWhen grouping\
                  \ is used the DecisionActions will not apply unless they all match."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [decision, frozen, inactive]
            x-redacted: []
        application/xml:
          schema:
            properties:
              decision: {description: The identifier of the Decision to which this resource
                  applies}
              action:
                description: The action to take when this check fails.
                enum: ['1', '3', '4', '5', '8']
                x-enum-descriptions: ['Return an error and decline. For transactions,
                    this results in a permanent decline. For merchants, they may still
                    be approved and the decline removed later on.', 'For txn decision:
                    Hold. Hold the Transaction. It will not be captured until it is
                    manually released.', 'For txn decision: Reserve. Reserve the Transaction.
                    The funds for the transaction will not be released until the Transaction
                    is manually reviewed.', This decision results in the merchant's
                    current activity (either transaction or payout) being declined.,
                  '']
              application:
                description: Where the action defined by this DecisionAction should
                  apply
                enum: [account, txn, entity]
                x-enum-descriptions: ['', Applies the action to the transaction, Applies
                    the action on the entity]
              scoreType:
                description: The type of score result on the related Decision
                enum: [low, high, none]
                x-enum-descriptions: ['', '', '']
              type:
                description: The type of DecisionAction
                enum: [equal, notEqual, contains, greater, less]
                x-enum-descriptions: ['', '', '', '', '']
              field: {description: The result field to check}
              data: {}
              message: {}
              code: {}
              score: {description: The score to check.}
              grouping: {description: 'A name for a group of decisionActions to be applied
                  in conjunction when evaluating this DecisionAction; when grouping
                  is used, the DecisionActions will not apply unless they all match.'}
              inactive:
                description: "Whether this resource is marked as inactive.\n<details>\n\
                  <summary>Valid Values</summary>\n\n- `0` - **Active** \n\n- `1` -\
                  \ **Inactive**\n</details>\n"
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: "Whether this resource is marked as frozen.\n<details>\n\
                  <summary>Valid Values</summary>\n\n- `0` - **Not Frozen** \n\n- `1`\
                  \ - **Frozen**\n</details>\n"
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [decision, frozen, inactive]
            x-redacted: []
        multipart/form-data:
          schema:
            properties:
              decision: {description: The identifier of the Decision to which this resource
                  applies}
              action:
                description: The action to take when this check fails.
                enum: ['1', '3', '4', '5', '8']
                x-enum-descriptions: ['Return an error and decline. For transactions,
                    this results in a permanent decline. For merchants, they may still
                    be approved and the decline removed later on.', 'For txn decision:
                    Hold. Hold the Transaction. It will not be captured until it is
                    manually released.', 'For txn decision: Reserve. Reserve the Transaction.
                    The funds for the transaction will not be released until the Transaction
                    is manually reviewed.', This decision results in the merchant's
                    current activity (either transaction or payout) being declined.,
                  '']
              application:
                description: Where the action defined by this DecisionAction should
                  apply
                enum: [account, txn, entity]
                x-enum-descriptions: ['', Applies the action to the transaction, Applies
                    the action on the entity]
              scoreType:
                description: The type of score result on the related Decision
                enum: [low, high, none]
                x-enum-descriptions: ['', '', '']
              type:
                description: The type of DecisionAction
                enum: [equal, notEqual, contains, greater, less]
                x-enum-descriptions: ['', '', '', '', '']
              field: {description: The result field to check}
              data: {}
              message: {}
              code: {}
              score: {description: The score to check.}
              grouping:
                description: "A name for a group of decisionActions to be applied in\
                  \ conjunction when evaluating this DecisionAction. \nWhen grouping\
                  \ is used the DecisionActions will not apply unless they all match."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [decision, frozen, inactive]
            x-redacted: []
        text/json:
          schema:
            properties:
              decision: {description: The identifier of the Decision to which this resource
                  applies}
              action:
                description: The action to take when this check fails.
                enum: ['1', '3', '4', '5', '8']
                x-enum-descriptions: ['Return an error and decline. For transactions,
                    this results in a permanent decline. For merchants, they may still
                    be approved and the decline removed later on.', 'For txn decision:
                    Hold. Hold the Transaction. It will not be captured until it is
                    manually released.', 'For txn decision: Reserve. Reserve the Transaction.
                    The funds for the transaction will not be released until the Transaction
                    is manually reviewed.', This decision results in the merchant's
                    current activity (either transaction or payout) being declined.,
                  '']
              application:
                description: Where the action defined by this DecisionAction should
                  apply
                enum: [account, txn, entity]
                x-enum-descriptions: ['', Applies the action to the transaction, Applies
                    the action on the entity]
              scoreType:
                description: The type of score result on the related Decision
                enum: [low, high, none]
                x-enum-descriptions: ['', '', '']
              type:
                description: The type of DecisionAction
                enum: [equal, notEqual, contains, greater, less]
                x-enum-descriptions: ['', '', '', '', '']
              field: {description: The result field to check}
              data: {}
              message: {}
              code: {}
              score: {description: The score to check.}
              grouping:
                description: "A name for a group of decisionActions to be applied in\
                  \ conjunction when evaluating this DecisionAction. \nWhen grouping\
                  \ is used the DecisionActions will not apply unless they all match."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [decision, frozen, inactive]
            x-redacted: []
        text/xml:
          schema:
            properties:
              decision: {description: The identifier of the Decision to which this resource
                  applies}
              action:
                description: The action to take when this check fails.
                enum: ['1', '3', '4', '5', '8']
                x-enum-descriptions: ['Return an error and decline. For transactions,
                    this results in a permanent decline. For merchants, they may still
                    be approved and the decline removed later on.', 'For txn decision:
                    Hold. Hold the Transaction. It will not be captured until it is
                    manually released.', 'For txn decision: Reserve. Reserve the Transaction.
                    The funds for the transaction will not be released until the Transaction
                    is manually reviewed.', This decision results in the merchant's
                    current activity (either transaction or payout) being declined.,
                  '']
              application:
                description: Where the action defined by this DecisionAction should
                  apply
                enum: [account, txn, entity]
                x-enum-descriptions: ['', Applies the action to the transaction, Applies
                    the action on the entity]
              scoreType:
                description: The type of score result on the related Decision
                enum: [low, high, none]
                x-enum-descriptions: ['', '', '']
              type:
                description: The type of DecisionAction
                enum: [equal, notEqual, contains, greater, less]
                x-enum-descriptions: ['', '', '', '', '']
              field: {description: The result field to check}
              data: {}
              message: {}
              code: {}
              score: {description: The score to check.}
              grouping:
                description: "A name for a group of decisionActions to be applied in\
                  \ conjunction when evaluating this DecisionAction. \nWhen grouping\
                  \ is used the DecisionActions will not apply unless they all match."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [decision, frozen, inactive]
            x-redacted: []
      description: Update Decision Action Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          decision: {description: The identifier of the Decision to
                              which this resource applies}
                          action:
                            description: The action to take when this check fails.
                            enum: ['1', '3', '4', '5', '8']
                            x-enum-descriptions: ['Return an error and decline. For
                                transactions, this results in a permanent decline. For
                                merchants, they may still be approved and the decline
                                removed later on.', 'For txn decision: Hold. Hold the
                                Transaction. It will not be captured until it is manually
                                released.', 'For txn decision: Reserve. Reserve the
                                Transaction. The funds for the transaction will not
                                be released until the Transaction is manually reviewed.',
                              This decision results in the merchant's current activity
                                (either transaction or payout) being declined., '']
                          application:
                            description: Where the action defined by this DecisionAction
                              should apply
                            enum: [account, txn, entity]
                            x-enum-descriptions: ['', Applies the action to the transaction,
                              Applies the action on the entity]
                          scoreType:
                            description: The type of score result on the related Decision
                            enum: [low, high, none]
                            x-enum-descriptions: ['', '', '']
                          type:
                            description: The type of DecisionAction
                            enum: [equal, notEqual, contains, greater, less]
                            x-enum-descriptions: ['', '', '', '', '']
                          field: {description: The result field to check}
                          score: {description: The score to check.}
                          data: {}
                          message: {}
                          code: {}
                          grouping: {description: 'A name for a group of decisionActions
                              to be applied in conjunction when evaluating this DecisionAction;
                              when grouping is used, the DecisionActions will not apply
                              unless they all match.'}
                          inactive:
                            description: "Whether this resource is marked as inactive.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Active** \n\n- `1` - **Inactive**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: "Whether this resource is marked as frozen.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Not Frozen** \n\n- `1` - **Frozen**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          decision: {description: The identifier of the Decision to
                              which this resource applies}
                          action:
                            description: The action to take when this check fails.
                            enum: ['1', '3', '4', '5', '8']
                            x-enum-descriptions: ['Return an error and decline. For
                                transactions, this results in a permanent decline. For
                                merchants, they may still be approved and the decline
                                removed later on.', 'For txn decision: Hold. Hold the
                                Transaction. It will not be captured until it is manually
                                released.', 'For txn decision: Reserve. Reserve the
                                Transaction. The funds for the transaction will not
                                be released until the Transaction is manually reviewed.',
                              This decision results in the merchant's current activity
                                (either transaction or payout) being declined., '']
                          application:
                            description: Where the action defined by this DecisionAction
                              should apply
                            enum: [account, txn, entity]
                            x-enum-descriptions: ['', Applies the action to the transaction,
                              Applies the action on the entity]
                          scoreType:
                            description: The type of score result on the related Decision
                            enum: [low, high, none]
                            x-enum-descriptions: ['', '', '']
                          type:
                            description: The type of DecisionAction
                            enum: [equal, notEqual, contains, greater, less]
                            x-enum-descriptions: ['', '', '', '', '']
                          field: {description: The result field to check}
                          score: {description: The score to check.}
                          data: {}
                          message: {}
                          code: {}
                          grouping: {description: 'A name for a group of decisionActions
                              to be applied in conjunction when evaluating this DecisionAction;
                              when grouping is used, the DecisionActions will not apply
                              unless they all match.'}
                          inactive:
                            description: "Whether this resource is marked as inactive.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Active** \n\n- `1` - **Inactive**\n</details>\n."
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: "Whether this resource is marked as frozen.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Not Frozen** \n\n- `1` - **Frozen**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: 'Response for update on Decision Actions resource: Update Decision
          Action Response'
      '400':
        content:
          application/json:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
          application/xml:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
        description: Error
    summary: Update Decision Action by it's ID.
    tags: [Decision Actions]
    operationId: putDecisionActionsId
  delete:
    description: Delete a Decision Action. A Decision Action is a piece of conditional
      logic that makes a VerificationResult change the application and the decision
      action in certain circumstances.
    parameters:
    - description: The ID of this resource and The Decision Action ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {}
      style: simple
    - description: "Set this path parameter to retrieve nested objects and return them\
        \ as part of the response. \nThe parameter name that you specify determines\
        \ the resources to return. \nFor example, set '?expand[login][]' to return a\
        \ nested login resource."
      explode: true
      in: query
      name: expand
      required: false
      schema: {}
      style: form
    - description: "Set this path parameter to retrieve unmasked details in the response.\
        \ \nThe fields specified will be returned in full. Only works for single-record\
        \ requests."
      explode: true
      in: query
      name: unmask
      required: false
      schema: {}
      style: form
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          decision: {description: The identifier of the Decision to
                              which this resource applies}
                          action:
                            description: The action to take when this check fails.
                            enum: ['1', '3', '4', '5', '8']
                            x-enum-descriptions: ['Return an error and decline. For
                                transactions, this results in a permanent decline. For
                                merchants, they may still be approved and the decline
                                removed later on.', 'For txn decision: Hold. Hold the
                                Transaction. It will not be captured until it is manually
                                released.', 'For txn decision: Reserve. Reserve the
                                Transaction. The funds for the transaction will not
                                be released until the Transaction is manually reviewed.',
                              This decision results in the merchant's current activity
                                (either transaction or payout) being declined., '']
                          application:
                            description: Where the action defined by this DecisionAction
                              should apply
                            enum: [account, txn, entity]
                            x-enum-descriptions: ['', Applies the action to the transaction,
                              Applies the action on the entity]
                          scoreType:
                            description: The type of score result on the related Decision
                            enum: [low, high, none]
                            x-enum-descriptions: ['', '', '']
                          type:
                            description: The type of DecisionAction
                            enum: [equal, notEqual, contains, greater, less]
                            x-enum-descriptions: ['', '', '', '', '']
                          field: {description: The result field to check}
                          score: {description: The score to check.}
                          data: {}
                          message: {}
                          code: {}
                          grouping: {description: 'A name for a group of decisionActions
                              to be applied in conjunction when evaluating this DecisionAction;
                              when grouping is used, the DecisionActions will not apply
                              unless they all match.'}
                          inactive:
                            description: "Whether this resource is marked as inactive.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Active** \n\n- `1` - **Inactive**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: "Whether this resource is marked as frozen.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Not Frozen** \n\n- `1` - **Frozen**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          decision: {description: The identifier of the Decision to
                              which this resource applies}
                          action:
                            description: The action to take when this check fails.
                            enum: ['1', '3', '4', '5', '8']
                            x-enum-descriptions: ['Return an error and decline. For
                                transactions, this results in a permanent decline. For
                                merchants, they may still be approved and the decline
                                removed later on.', 'For txn decision: Hold. Hold the
                                Transaction. It will not be captured until it is manually
                                released.', 'For txn decision: Reserve. Reserve the
                                Transaction. The funds for the transaction will not
                                be released until the Transaction is manually reviewed.',
                              This decision results in the merchant's current activity
                                (either transaction or payout) being declined., '']
                          application:
                            description: Where the action defined by this DecisionAction
                              should apply
                            enum: [account, txn, entity]
                            x-enum-descriptions: ['', Applies the action to the transaction,
                              Applies the action on the entity]
                          scoreType:
                            description: The type of score result on the related Decision
                            enum: [low, high, none]
                            x-enum-descriptions: ['', '', '']
                          type:
                            description: The type of DecisionAction
                            enum: [equal, notEqual, contains, greater, less]
                            x-enum-descriptions: ['', '', '', '', '']
                          field: {description: The result field to check}
                          score: {description: The score to check.}
                          data: {}
                          message: {}
                          code: {}
                          grouping: {description: 'A name for a group of decisionActions
                              to be applied in conjunction when evaluating this DecisionAction;
                              when grouping is used, the DecisionActions will not apply
                              unless they all match.'}
                          inactive:
                            description: "Whether this resource is marked as inactive.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Active** \n\n- `1` - **Inactive**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: "Whether this resource is marked as frozen.\n\
                              <details>\n<summary>Valid Values</summary>\n\n- `0` -\
                              \ **Not Frozen** \n\n- `1` - **Frozen**\n</details>\n"
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: Delete Decision Action Response for decisionActions resource.
      '400':
        content:
          application/json:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
          application/xml:
            schema:
              description: An array of zero or more errors that occurred when the API
                processed the request.
              items:
                properties:
                  code: {description: The error code associated with a particular error.}
                  severity:
                    description: "The severity level of a particular error. \nPossible\
                      \ values are '0' (informational), '1' (warning), '2' (error) and\
                      \ '4' (failure)."
                  message: {description: The message associated with a particular error.}
                  errorCode: {description: An identifying string code for this error.}
        description: Error
    summary: Delete Decision Action by it's ID.
    tags: [Decision Actions]
    operationId: deleteDecisionActionsId
