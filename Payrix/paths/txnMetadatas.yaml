  get:
    description: "Query a txnMetadatas resource. \nA txnMetadatas resource represents\
      \ data from an EMV Transaction and/or returned by the credit card network associated\
      \ with a Transaction."
    parameters:
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    - {$ref: ../components/schemas/requestQueryPageNumberParams.yaml}
    - {$ref: ../components/schemas/requestQueryPageLimitParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/txnMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/txnMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for query on txnMetadatas resource
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve all transaction metadata (txnMetadatas).
    tags: [Transactions (Txns)]
    operationId: getTxnMetadatas
  post:
    description: "Create a txnMetadatas resource. \nA txnMetadatas resource represents\
      \ data from an EMV Transaction and/or returned by the credit card network associated\
      \ with a Transaction."
    requestBody:
      content:
        application/json:
          schema:
            {$ref: ../components/schemas/models/txnMetadatasPostReqBodySchema.yaml}
        application/x-www-form-urlencoded:
          schema:
            {$ref: ../components/schemas/models/txnMetadatasPostReqBodySchema.yaml}
        application/xml:
          schema:
            {$ref: ../components/schemas/models/txnMetadatasPostReqBodySchema.yaml}
        multipart/form-data:
          schema:
            {$ref: ../components/schemas/models/txnMetadatasPostReqBodySchema.yaml}
        text/json:
          schema:
            {$ref: ../components/schemas/models/txnMetadatasPostReqBodySchema.yaml}
        text/xml:
          schema:
            {$ref: ../components/schemas/models/txnMetadatasPostReqBodySchema.yaml}
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/txnMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/txnMetadatasModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for create on txnMetadatas resource
      '400':
        content:
          { $ref: ../components/schemas/error400ResponseContent.yaml }
        description: Error
    tags: [Transactions (Txns)]
    operationId: postTxnMetadatas
