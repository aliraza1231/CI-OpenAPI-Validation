  get:
    description: "Query a Watchlist. A Watchlist is a list of WatchlistItems. \nWatchlistItems\
      \ will be associated with a Watchlist and can be used in Decisions."
    parameters:
    - description: The ID of this resource.
      explode: false
      in: path
      name: id
      required: true
      schema: {}
      style: simple
    - description: "Set this path parameter to retrieve nested objects and return them\
        \ as part of the response. \nThe parameter name that you specify determines\
        \ the resources to return. \nFor example, set '?expand[login][]' to return a\
        \ nested login resource."
      explode: true
      in: query
      name: expand
      required: false
      schema: {}
      style: form
    - description: "Set this path parameter to retrieve unmasked details in the response.\
        \ \nThe fields specified will be returned in full. Only works for single-record\
        \ requests."
      explode: true
      in: query
      name: unmask
      required: false
      schema: {}
      style: form
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The identifier of the Login associated
                              with this Watchlist.}
                          integration:
                            description: The integration related to this Watchlist.
                            enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC,
                              PAYRIX, PLAID, SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA,
                              VANTIV, VCORE, WEBSHIELD, WELLSACH, WELLSFARGO, WFSINGLE,
                              WORLDPAY]
                            x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC,
                              '', '', SIFT, SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD,
                              '', WELLSFARGO, '', '']
                          name:
                            description: "The name of this Watchlist. \nThis field is\
                              \ stored as a text string and must be between 0 and 500\
                              \ characters long."
                          description:
                            description: "A description of this Watchlist. \nThis field\
                              \ is stored as a text string and must be between 0 and\
                              \ 500 characters long."
                          default:
                            description: Whether this Watchlist applies to everyone.
                            enum: ['0', '1']
                            x-enum-descriptions: [Non-Default, Default]
                          custom:
                            description: "A custom identifier for this Watchlist. \n\
                              This field is stored as a text string and must be between\
                              \ 0 and 500 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The identifier of the Login associated
                              with this Watchlist.}
                          integration:
                            description: The integration related to this Watchlist.
                            enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC,
                              PAYRIX, PLAID, SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA,
                              VANTIV, VCORE, WEBSHIELD, WELLSACH, WELLSFARGO, WFSINGLE,
                              WORLDPAY]
                            x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC,
                              '', '', SIFT, SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD,
                              '', WELLSFARGO, '', '']
                          name:
                            description: "The name of this Watchlist. \nThis field is\
                              \ stored as a text string and must be between 0 and 500\
                              \ characters long."
                          description:
                            description: "A description of this Watchlist. \nThis field\
                              \ is stored as a text string and must be between 0 and\
                              \ 500 characters long."
                          default:
                            description: Whether this Watchlist applies to everyone.
                            enum: ['0', '1']
                            x-enum-descriptions: [Non-Default, Default]
                          custom:
                            description: "A custom identifier for this Watchlist. \n\
                              This field is stored as a text string and must be between\
                              \ 0 and 500 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: Response for query on watchlists resource
    tags: [Watchlists]
    operationId: getWatchlistsId
  put:
    description: "Update a Watchlist. A Watchlist is a list of WatchlistItems. \nWatchlistItems\
      \ will be associated with a Watchlist and can be used in Decisions."
    parameters:
    - description: The ID of this resource.
      explode: false
      in: path
      name: id
      required: true
      schema: {}
      style: simple
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    requestBody:
      content:
        application/json:
          schema:
            properties:
              login: {description: The identifier of the Login associated with this
                  Watchlist.}
              integration:
                description: The integration related to this Watchlist.
                enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC, PAYRIX, PLAID,
                  SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA, VANTIV, VCORE, WEBSHIELD,
                  WELLSACH, WELLSFARGO, WFSINGLE, WORLDPAY]
                x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC, '', '', SIFT,
                  SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD, '', WELLSFARGO,
                  '', '']
              name:
                description: "The name of this Watchlist. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              description:
                description: "A description of this Watchlist. \nThis field is stored\
                  \ as a text string and must be between 0 and 500 characters long."
              default:
                description: Whether this Watchlist applies to everyone.
                enum: ['0', '1']
                x-enum-descriptions: [Non-Default, Default]
              custom:
                description: "A custom identifier for this Watchlist. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [default, frozen, inactive, login]
            x-redacted: []
        application/x-www-form-urlencoded:
          schema:
            properties:
              login: {description: The identifier of the Login associated with this
                  Watchlist.}
              integration:
                description: The integration related to this Watchlist.
                enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC, PAYRIX, PLAID,
                  SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA, VANTIV, VCORE, WEBSHIELD,
                  WELLSACH, WELLSFARGO, WFSINGLE, WORLDPAY]
                x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC, '', '', SIFT,
                  SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD, '', WELLSFARGO,
                  '', '']
              name:
                description: "The name of this Watchlist. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              description:
                description: "A description of this Watchlist. \nThis field is stored\
                  \ as a text string and must be between 0 and 500 characters long."
              default:
                description: Whether this Watchlist applies to everyone.
                enum: ['0', '1']
                x-enum-descriptions: [Non-Default, Default]
              custom:
                description: "A custom identifier for this Watchlist. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [default, frozen, inactive, login]
            x-redacted: []
        application/xml:
          schema:
            properties:
              login: {description: The identifier of the Login associated with this
                  Watchlist.}
              integration:
                description: The integration related to this Watchlist.
                enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC, PAYRIX, PLAID,
                  SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA, VANTIV, VCORE, WEBSHIELD,
                  WELLSACH, WELLSFARGO, WFSINGLE, WORLDPAY]
                x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC, '', '', SIFT,
                  SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD, '', WELLSFARGO,
                  '', '']
              name:
                description: "The name of this Watchlist. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              description:
                description: "A description of this Watchlist. \nThis field is stored\
                  \ as a text string and must be between 0 and 500 characters long."
              default:
                description: Whether this Watchlist applies to everyone.
                enum: ['0', '1']
                x-enum-descriptions: [Non-Default, Default]
              custom:
                description: "A custom identifier for this Watchlist. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [default, frozen, inactive, login]
            x-redacted: []
        multipart/form-data:
          schema:
            properties:
              login: {description: The identifier of the Login associated with this
                  Watchlist.}
              integration:
                description: The integration related to this Watchlist.
                enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC, PAYRIX, PLAID,
                  SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA, VANTIV, VCORE, WEBSHIELD,
                  WELLSACH, WELLSFARGO, WFSINGLE, WORLDPAY]
                x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC, '', '', SIFT,
                  SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD, '', WELLSFARGO,
                  '', '']
              name:
                description: "The name of this Watchlist. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              description:
                description: "A description of this Watchlist. \nThis field is stored\
                  \ as a text string and must be between 0 and 500 characters long."
              default:
                description: Whether this Watchlist applies to everyone.
                enum: ['0', '1']
                x-enum-descriptions: [Non-Default, Default]
              custom:
                description: "A custom identifier for this Watchlist. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [default, frozen, inactive, login]
            x-redacted: []
        text/json:
          schema:
            properties:
              login: {description: The identifier of the Login associated with this
                  Watchlist.}
              integration:
                description: The integration related to this Watchlist.
                enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC, PAYRIX, PLAID,
                  SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA, VANTIV, VCORE, WEBSHIELD,
                  WELLSACH, WELLSFARGO, WFSINGLE, WORLDPAY]
                x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC, '', '', SIFT,
                  SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD, '', WELLSFARGO,
                  '', '']
              name:
                description: "The name of this Watchlist. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              description:
                description: "A description of this Watchlist. \nThis field is stored\
                  \ as a text string and must be between 0 and 500 characters long."
              default:
                description: Whether this Watchlist applies to everyone.
                enum: ['0', '1']
                x-enum-descriptions: [Non-Default, Default]
              custom:
                description: "A custom identifier for this Watchlist. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [default, frozen, inactive, login]
            x-redacted: []
        text/xml:
          schema:
            properties:
              login: {description: The identifier of the Login associated with this
                  Watchlist.}
              integration:
                description: The integration related to this Watchlist.
                enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC, PAYRIX, PLAID,
                  SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA, VANTIV, VCORE, WEBSHIELD,
                  WELLSACH, WELLSFARGO, WFSINGLE, WORLDPAY]
                x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC, '', '', SIFT,
                  SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD, '', WELLSFARGO,
                  '', '']
              name:
                description: "The name of this Watchlist. \nThis field is stored as\
                  \ a text string and must be between 0 and 500 characters long."
              description:
                description: "A description of this Watchlist. \nThis field is stored\
                  \ as a text string and must be between 0 and 500 characters long."
              default:
                description: Whether this Watchlist applies to everyone.
                enum: ['0', '1']
                x-enum-descriptions: [Non-Default, Default]
              custom:
                description: "A custom identifier for this Watchlist. \nThis field is\
                  \ stored as a text string and must be between 0 and 500 characters\
                  \ long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [default, frozen, inactive, login]
            x-redacted: []
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The identifier of the Login associated
                              with this Watchlist.}
                          integration:
                            description: The integration related to this Watchlist.
                            enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC,
                              PAYRIX, PLAID, SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA,
                              VANTIV, VCORE, WEBSHIELD, WELLSACH, WELLSFARGO, WFSINGLE,
                              WORLDPAY]
                            x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC,
                              '', '', SIFT, SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD,
                              '', WELLSFARGO, '', '']
                          name:
                            description: "The name of this Watchlist. \nThis field is\
                              \ stored as a text string and must be between 0 and 500\
                              \ characters long."
                          description:
                            description: "A description of this Watchlist. \nThis field\
                              \ is stored as a text string and must be between 0 and\
                              \ 500 characters long."
                          default:
                            description: Whether this Watchlist applies to everyone.
                            enum: ['0', '1']
                            x-enum-descriptions: [Non-Default, Default]
                          custom:
                            description: "A custom identifier for this Watchlist. \n\
                              This field is stored as a text string and must be between\
                              \ 0 and 500 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The identifier of the Login associated
                              with this Watchlist.}
                          integration:
                            description: The integration related to this Watchlist.
                            enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC,
                              PAYRIX, PLAID, SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA,
                              VANTIV, VCORE, WEBSHIELD, WELLSACH, WELLSFARGO, WFSINGLE,
                              WORLDPAY]
                            x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC,
                              '', '', SIFT, SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD,
                              '', WELLSFARGO, '', '']
                          name:
                            description: "The name of this Watchlist. \nThis field is\
                              \ stored as a text string and must be between 0 and 500\
                              \ characters long."
                          description:
                            description: "A description of this Watchlist. \nThis field\
                              \ is stored as a text string and must be between 0 and\
                              \ 500 characters long."
                          default:
                            description: Whether this Watchlist applies to everyone.
                            enum: ['0', '1']
                            x-enum-descriptions: [Non-Default, Default]
                          custom:
                            description: "A custom identifier for this Watchlist. \n\
                              This field is stored as a text string and must be between\
                              \ 0 and 500 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: Response for update on watchlists resource
    tags: [Watchlists]
    operationId: putWatchlistsId
  delete:
    description: "Delete a Watchlist. A Watchlist is a list of WatchlistItems. \nWatchlistItems\
      \ will be associated with a Watchlist and can be used in Decisions."
    parameters:
    - description: The ID of this resource.
      explode: false
      in: path
      name: id
      required: true
      schema: {}
      style: simple
    - description: "Set this path parameter to retrieve nested objects and return them\
        \ as part of the response. \nThe parameter name that you specify determines\
        \ the resources to return. \nFor example, set '?expand[login][]' to return a\
        \ nested login resource."
      explode: true
      in: query
      name: expand
      required: false
      schema: {}
      style: form
    - description: "Set this path parameter to retrieve unmasked details in the response.\
        \ \nThe fields specified will be returned in full. Only works for single-record\
        \ requests."
      explode: true
      in: query
      name: unmask
      required: false
      schema: {}
      style: form
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The identifier of the Login associated
                              with this Watchlist.}
                          integration:
                            description: The integration related to this Watchlist.
                            enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC,
                              PAYRIX, PLAID, SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA,
                              VANTIV, VCORE, WEBSHIELD, WELLSACH, WELLSFARGO, WFSINGLE,
                              WORLDPAY]
                            x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC,
                              '', '', SIFT, SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD,
                              '', WELLSFARGO, '', '']
                          name:
                            description: "The name of this Watchlist. \nThis field is\
                              \ stored as a text string and must be between 0 and 500\
                              \ characters long."
                          description:
                            description: "A description of this Watchlist. \nThis field\
                              \ is stored as a text string and must be between 0 and\
                              \ 500 characters long."
                          default:
                            description: Whether this Watchlist applies to everyone.
                            enum: ['0', '1']
                            x-enum-descriptions: [Non-Default, Default]
                          custom:
                            description: "A custom identifier for this Watchlist. \n\
                              This field is stored as a text string and must be between\
                              \ 0 and 500 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          login: {description: The identifier of the Login associated
                              with this Watchlist.}
                          integration:
                            description: The integration related to this Watchlist.
                            enum: [APPLE, ELAVON, FEDACH, FIRSTDATA, NEUTRINO, OFAC,
                              PAYRIX, PLAID, SIFT, SOCURE, SOUNDPAYMENTS, TDBANK, TDBANKCA,
                              VANTIV, VCORE, WEBSHIELD, WELLSACH, WELLSFARGO, WFSINGLE,
                              WORLDPAY]
                            x-enum-descriptions: ['', ELAVON, '', '', NEUTRINO, OFAC,
                              '', '', SIFT, SOCURE, '', TDBANK, '', VANTIV, VCORE, WEBSHIELD,
                              '', WELLSFARGO, '', '']
                          name:
                            description: "The name of this Watchlist. \nThis field is\
                              \ stored as a text string and must be between 0 and 500\
                              \ characters long."
                          description:
                            description: "A description of this Watchlist. \nThis field\
                              \ is stored as a text string and must be between 0 and\
                              \ 500 characters long."
                          default:
                            description: Whether this Watchlist applies to everyone.
                            enum: ['0', '1']
                            x-enum-descriptions: [Non-Default, Default]
                          custom:
                            description: "A custom identifier for this Watchlist. \n\
                              This field is stored as a text string and must be between\
                              \ 0 and 500 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: Response for delete on watchlists resource
    tags: [Watchlists]
    operationId: deleteWatchlistsId
