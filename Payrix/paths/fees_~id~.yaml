  get:
    description: Query a Fee that is an amount one Entity can charge another Entity
      according to a particular schedule, or based on certain events.
    parameters:
    - description: The ID of this resource and The Fee ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - { $ref: ../components/schemas/requestQueryExpandParams.yaml }
    - { $ref: ../components/schemas/requestQueryUnmaskParams.yaml }
    - { $ref: ../components/schemas/requestHeaderSearchParams.yaml }
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/feesModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/feesModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Retrieve fee response for query on fees resource.
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve a Fee by it's ID.
    tags: [Fees]
    operationId: getFeesId
  put:
    description: Update a Fee that is an amount one Entity can charge another Entity
      according to a particular schedule, or based on certain events.
    parameters:
    - description: The ID of this resource and The Fee ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    requestBody:
      content:
        application/json:
          schema:
            {$ref: ../components/schemas/models/feesPut&PostReqBodySchemaModel.yaml}
        application/x-www-form-urlencoded:
          schema:
            {$ref: ../components/schemas/models/feesPut&PostReqBodySchemaModel.yaml}
        application/xml:
          schema:
            {$ref: ../components/schemas/models/feesPut&PostReqBodySchemaModel.yaml}
        multipart/form-data:
          schema:
            {$ref: ../components/schemas/models/feesPut&PostReqBodySchemaModel.yaml}
        text/json:
          schema:
            {$ref: ../components/schemas/models/feesPut&PostReqBodySchemaModel.yaml}
        text/xml:
          schema:
            {$ref: ../components/schemas/models/feesPut&PostReqBodySchemaModel.yaml}
      description: Update Fee Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/feesModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/feesModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Response for update on fees resource Update Fee Response
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Update a Fee by it's ID.
    tags: [Fees]
    operationId: putFeesId
  delete:
    description: Delete a Fee that is an amount one Entity can charge another Entity
      according to a particular schedule, or based on certain events.
    parameters:
    - description: The ID of this resource and The Fee ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - { $ref: ../components/schemas/requestQueryExpandParams.yaml }
    - { $ref: ../components/schemas/requestQueryUnmaskParams.yaml }
    - { $ref: ../components/schemas/requestHeaderSearchParams.yaml }
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/feesModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/feesModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: The response for delete on fees resource is a Delete Fee Response.
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Delete a Fee by it's ID.
    tags: [Fees]
    operationId: deleteFeesId
