  get:
    description: "Query a Facilitator resource. \nA Facilitator resource represents\
      \ a payment facilitator that processes payments for a Partition. \nYou can only\
      \ associate one Facilitator to each Partition."
    parameters:
    - description: "Set this path parameter to retrieve nested objects and return them\
        \ as part of the response. \nThe parameter name that you specify determines\
        \ the resources to return. \nFor example, set '?expand[login][]' to return a\
        \ nested login resource."
      explode: true
      in: query
      name: expand
      required: false
      schema: {}
      style: form
    - description: "Set this path parameter to retrieve unmasked details in the response.\
        \ \nThe fields specified will be returned in full. Only works for single-record\
        \ requests."
      explode: true
      in: query
      name: unmask
      required: false
      schema: {}
      style: form
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    - description: "Set this path parameter to request a specific page of records. \n\
        For example, set '?page[number]=2' to retrieve the second page of records for\
        \ this request."
      explode: true
      in: query
      name: page[number]
      required: false
      schema: {}
      style: form
    - description: |-
        Set this path parameter to request up to a specific amount of records. By default 30 records are retrieved per page. The maximum limit that can be set is 100.
        For example, set '?page[limit]=50' to retrieve up to 50 records for this request.
      explode: true
      in: query
      name: page[limit]
      required: false
      schema: {}
      style: form
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          entity: {description: The identifier of the Entity that this
                              Facilitators resource belongs to.}
                          prefix:
                            description: "The billing descriptor prefix for this Facilitator.\
                              \ \nThis string appears at the start of the description\
                              \ for transactions on credit card statements when any\
                              \ sub-Merchant of this Facilitator charges a card."
                          partition:
                            description: "The identifier of the Partition that this\
                              \ Facilitator is associated with. \nYou can only associate\
                              \ one Facilitator to each Partition."
                          gatewayName:
                            description: "The name of the credit card processor. \n\
                              Optional field used by transaction fraud checking."
                          tcVersion: {description: An indicator showing the version
                              of the terms and conditions that this Facilitator wants
                              their sub-Merchants to accept.}
                          tcMerchant: {}
                          tcVendor: {}
                          chargebackNotificationEmail: {}
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          entity: {description: The identifier of the Entity that this
                              Facilitators resource belongs to.}
                          prefix:
                            description: "The billing descriptor prefix for this Facilitator.\
                              \ \nThis string appears at the start of the description\
                              \ for transactions on credit card statements when any\
                              \ sub-Merchant of this Facilitator charges a card."
                          partition:
                            description: "The identifier of the Partition that this\
                              \ Facilitator is associated with. \nYou can only associate\
                              \ one Facilitator to each Partition."
                          gatewayName:
                            description: "The name of the credit card processor. \n\
                              Optional field used by transaction fraud checking."
                          tcVersion: {description: An indicator showing the version
                              of the terms and conditions that this Facilitator wants
                              their sub-Merchants to accept.}
                          tcMerchant: {}
                          tcVendor: {}
                          chargebackNotificationEmail: {}
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: Response for query on facilitators resource
    tags: [Facilitator]
    operationId: getFacilitators
  post:
    description: "Create a Facilitator resource. \nA Facilitator resource represents\
      \ a payment facilitator that processes payments for a Partition. \nYou can only\
      \ associate one Facilitator to each Partition."
    requestBody:
      content:
        application/json:
          schema:
            properties: {$ref: ../components/schemas/models/facilitatorsModel.yaml}
            required: [entity, frozen, inactive, partition, prefix]
            x-redacted: []
        application/x-www-form-urlencoded:
          schema:
            properties: {$ref: ../components/schemas/models/facilitatorsModel.yaml}
            required: [entity, frozen, inactive, partition, prefix]
            x-redacted: []
        application/xml:
          schema:
            properties: {$ref: ../components/schemas/models/facilitatorsModel.yaml}
            required: [entity, frozen, inactive, partition, prefix]
            x-redacted: []
        multipart/form-data:
          schema:
            properties: {$ref: ../components/schemas/models/facilitatorsModel.yaml}
            required: [entity, frozen, inactive, partition, prefix]
            x-redacted: []
        text/json:
          schema:
            properties: {$ref: ../components/schemas/models/facilitatorsModel.yaml}
            required: [entity, frozen, inactive, partition, prefix]
            x-redacted: []
        text/xml:
          schema:
            properties: {$ref: ../components/schemas/models/facilitatorsModel.yaml}
            required: [entity, frozen, inactive, partition, prefix]
            x-redacted: []
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          entity: {description: The identifier of the Entity that this
                              Facilitators resource belongs to.}
                          prefix:
                            description: "The billing descriptor prefix for this Facilitator.\
                              \ \nThis string appears at the start of the description\
                              \ for transactions on credit card statements when any\
                              \ sub-Merchant of this Facilitator charges a card."
                          partition:
                            description: "The identifier of the Partition that this\
                              \ Facilitator is associated with. \nYou can only associate\
                              \ one Facilitator to each Partition."
                          gatewayName:
                            description: "The name of the credit card processor. \n\
                              Optional field used by transaction fraud checking."
                          tcVersion: {description: An indicator showing the version
                              of the terms and conditions that this Facilitator wants
                              their sub-Merchants to accept.}
                          tcMerchant: {}
                          tcVendor: {}
                          chargebackNotificationEmail: {}
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          entity: {description: The identifier of the Entity that this
                              Facilitators resource belongs to.}
                          prefix:
                            description: "The billing descriptor prefix for this Facilitator.\
                              \ \nThis string appears at the start of the description\
                              \ for transactions on credit card statements when any\
                              \ sub-Merchant of this Facilitator charges a card."
                          partition:
                            description: "The identifier of the Partition that this\
                              \ Facilitator is associated with. \nYou can only associate\
                              \ one Facilitator to each Partition."
                          gatewayName:
                            description: "The name of the credit card processor. \n\
                              Optional field used by transaction fraud checking."
                          tcVersion: {description: An indicator showing the version
                              of the terms and conditions that this Facilitator wants
                              their sub-Merchants to accept.}
                          tcMerchant: {}
                          tcVendor: {}
                          chargebackNotificationEmail: {}
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: Response for create on facilitators resource
    tags: [Facilitator]
    operationId: postFacilitators
