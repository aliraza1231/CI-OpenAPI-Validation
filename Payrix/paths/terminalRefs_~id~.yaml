  get:
    description: Query a terminalRefs resource, which represents a reference code issued
      by the Processor that represents a particular Terminal.
    parameters:
    - description: The ID of this resource, also known as The Terminal Reference ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestQueryExpandParams.yaml}
    - {$ref: ../components/schemas/requestQueryUnmaskParams.yaml}
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalRefsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalRefsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: Retrieve the Terminal Reference Response for query on terminalRefs
          resource.
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Retrieve a Terminal Reference by its ID.
    tags: [Terminals]
    operationId: getTerminalRefsId
  put:
    description: Update a terminalRefs resource, which represents a reference code issued
      by the Processor that represents a particular Terminal.
    parameters:
    - description: The ID of this resource, also known as The Terminal Reference ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - {$ref: ../components/schemas/requestHeaderSearchParams.yaml}
    requestBody:
      content:
        application/json:
          schema:
            {$ref: ../components/schemas/models/terminalRefsPutReqBodySchemaModel.yaml}
        application/x-www-form-urlencoded:
          schema:
            {$ref: ../components/schemas/models/terminalRefsPutReqBodySchemaModel.yaml}
        application/xml:
          schema:
            {$ref: ../components/schemas/models/terminalRefsPutReqBodySchemaModel.yaml}
        multipart/form-data:
          schema:
            {$ref: ../components/schemas/models/terminalRefsPutReqBodySchemaModel.yaml}
        text/json:
          schema:
            {$ref: ../components/schemas/models/terminalRefsPutReqBodySchemaModel.yaml}
        text/xml:
          schema:
            {$ref: ../components/schemas/models/terminalRefsPutReqBodySchemaModel.yaml}
      description: Update Terminal Reference Request
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalRefsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalRefsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/details.yaml}
        description: 'Response for update on Terminal References resource: Update Terminal
          Reference Response'
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Update a Terminal Reference by its ID.
    tags: [Terminals]
    operationId: putTerminalRefsId
  delete:
    description: Delete a terminalRefs resource, which represents a reference code issued
      by the Processor that represents a particular Terminal.
    parameters:
    - description: The ID of this resource, also known as The Terminal Reference ID.
      explode: false
      in: path
      name: id
      required: true
      schema: {type: string}
      style: simple
    - { $ref: ../components/schemas/requestQueryExpandParams.yaml }
    - { $ref: ../components/schemas/requestQueryUnmaskParams.yaml }
    - { $ref: ../components/schemas/requestHeaderSearchParams.yaml }
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalRefsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        {$ref: ../components/schemas/models/terminalRefsModel.yaml}
                    details: {$ref: ../components/schemas/details.yaml}
                    errors: {$ref: ../components/schemas/error.yaml}
        description: The Delete Terminal Reference Response for the terminalRefs resource.
      '400':
        content:
          {$ref: ../components/schemas/error400ResponseContent.yaml}
        description: Error
    summary: Delete a Terminal Reference by its ID.
    tags: [Terminals]
    operationId: deleteTerminalRefsId
