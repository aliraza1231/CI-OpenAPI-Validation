  get:
    description: "Query a ConfigurationStage resource. \nA ConfigurationStage resource\
      \ will hold relevant information about a certain stage of integration configuration."
    parameters:
    - description: "Set this path parameter to retrieve nested objects and return them\
        \ as part of the response. \nThe parameter name that you specify determines\
        \ the resources to return. \nFor example, set '?expand[login][]' to return a\
        \ nested login resource."
      explode: true
      in: query
      name: expand
      required: false
      schema: {}
      style: form
    - description: "Set this path parameter to retrieve unmasked details in the response.\
        \ \nThe fields specified will be returned in full. Only works for single-record\
        \ requests."
      explode: true
      in: query
      name: unmask
      required: false
      schema: {}
      style: form
    - description: "Set this header to filter or sort the list of resources that the\
        \ method returns. \nFor example, to find all resources that have a property\
        \ of 'public' set to '1', set this header to 'public[equals]=1'. You can use\
        \ the operators 'equals', 'greater', 'less', 'like', 'in' and 'sort'. To sort\
        \ the resources, use the keywords 'asc' (for an ascending sort) or 'desc' (for\
        \ a descending sort).\nYou may also use explicit 'and/or' querying by passing\
        \ arrays of or and arrays of and parameters. For example, to find all resources\
        \ that have a property of 'public' set to '1' or a property of 'active' set\
        \ to '0', set this header to 'or[][public][equals]=1&or[][active][equals]=0'."
      explode: false
      in: header
      name: search
      required: false
      schema: {}
      style: simple
    - description: "Set this path parameter to request a specific page of records. \n\
        For example, set '?page[number]=2' to retrieve the second page of records for\
        \ this request."
      explode: true
      in: query
      name: page[number]
      required: false
      schema: {}
      style: form
    - description: |-
        Set this path parameter to request up to a specific amount of records. By default 30 records are retrieved per page. The maximum limit that can be set is 100.
        For example, set '?page[limit]=50' to retrieve up to 50 records for this request.
      explode: true
      in: query
      name: page[limit]
      required: false
      schema: {}
      style: form
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          configuration: {description: The identifier of the Configuration
                              that owns this configurationStage resource.}
                          name:
                            description: The name of this configurationStage resource.
                            enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate,
                              applepay, balance, batch, chargeback, chargebackList,
                              confirmDisbursement, contactUpdate, createMerchant, credit,
                              decrypt, debit, debitPinEnable, debitPinlessEnable, discoverUrl,
                              discoverUpdate, document, documentList, interchange, encrypt,
                              entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                              expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                              memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                              merchantCardNotPresent, merchantUpdate, merchantRetrieval,
                              noc, nocReturns, omniTokenEnable, passTokenEnable, payment,
                              paymentConfirmation, paymentUpdate, payout, primaryMemberUpdate,
                              registerSrs, returnCredit, returnDebit, returns, saferPaymentEnable,
                              terminalCreate, terminalList, terminalsSupported, txn,
                              txnList, varsheet]
                            x-enum-descriptions: [Blank. Used for a stage with no name,
                              '', '', '', '', '', '', '', '', Chargeback. The standard
                                name used for chargeback related stages, ChargebackList.
                                The specific name to get chargeback reports, '', '',
                              '', '', '', '', '', '', '', '', 'Document. The standard
                                name for chargeback related documentation such as uploading
                                a document or updating a document ', DocumentList. Used
                                for receiving documents related to a chargeback, '',
                              '', Entity. The standard name used when it comes to entity
                                related stages such as retrieving an entity or updating,
                              '', EntityRetrieval. Used when entity stage name is already
                                being used in that crud configuration, EntityUpdate.
                                Used when entity stage name is already being used in
                                that crud configuration, '', Return. Used for return
                                stage name, FeeList. The specific name to get fees reports,
                              '', MemberCreate. Stage name used to indicate creating
                                a merchant member, MemberDelete. Stage name used to
                                indicate deleting a merchant member, Members. Stage
                                name used for creating several members for the merchant,
                              '', Merchant. The standard name used for merchant related
                                stages such retrieving a merchant or updating a merchant,
                              '', '', '', MerchantRetrieval. Used when merchant stage
                                name is already being used in that crud configuration,
                              Noc. Stage name used for notification of change for disbursement
                                related integrations, '', '', '', '', '', '', Payout.
                                Stage name used for payouts, '', '', '', '', '', '',
                              '', '', '', Txn. The standard name used for transaction
                                related stages, TxnList. The specific name to get transaction
                                reports, '']
                          method:
                            description: The method used for configurationStage resource.
                            enum: [sftpUpload, sftpDownload, post, get, put, delete,
                              tcp]
                            x-enum-descriptions: [SFtpUpload. Will use sftp to upload,
                              '', Post. Will use a post request, Get. Will use a get
                                request, Put. Will use a put request, Delete. Will use
                                a delete request, Tcp. Will use tcp protocol]
                          sequence: {}
                          input:
                            description: The input used for configurationStage resource.
                            enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                              APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT,
                              ELAVON_BATCH_READ_FLAT, ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV,
                              ELAVON_TXN_XML, FEDACH_REPORT_FLAT, FIRSTDATA_BATCH_FLAT,
                              FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON, FIRSTDATA_REPORTS_FLAT,
                              FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                              FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM,
                              NEUTRINO_JSON, OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON,
                              PAYRIX_ENCRYPTION_JSON, PAYRIX_GOOGLEPAY_DECRYPTION_JSON,
                              PAYRIX_TXN_JSON, PUSHY_TXN_JSON, SIFT_JSON, SOCURE_ALLOY_JSON,
                              SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM, SOUNDPAYMENTS_TXN_XML,
                              VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                              VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                              VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV,
                              VANTIV_TXN_XML, VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT,
                              VCORE_BINREPORT_FLAT, VCORE_BOARD_API, VCORE_BOARD_FLAT,
                              VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                              VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT,
                              VCORE_VARSHEET_JSON, WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT,
                              WELLSACH_PAYOUT_FLAT, WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML,
                              WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT, WFSINGLE_REPORT_FLAT,
                              WFSINGLE_BOARD_XML]
                            x-enum-descriptions: ['', '', '', '', '', '', '', '', '',
                              '', '', '', '', '', '', '', '', '', NACHA input file,
                              '', NEUTRINO_FORM input file, '', OFAC_XML input file,
                              '', '', '', '', '', '', SIFT_JSON input file, '', SOCURE_FORM
                                input file, SOCURE_JSON input file, SOUNDPAYMENTS_TXN_FORM
                                input file, SOUNDPAYMENTS_TXN_XML input file, VANTIV_BATCH_XML
                                input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                                input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                                input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV
                                input file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT
                                input file, VCORE_BATCH_READ_FLAT input file, '', '',
                              VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT input
                                file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                                input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM
                                input file, WEBSHIELD_JSON input file, '', '', '', '',
                              '', '', '', '']
                          output:
                            description: The output used for configurationStage resource.
                            enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                              APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT,
                              ELAVON_BATCH_READ_FLAT, ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV,
                              ELAVON_TXN_XML, FEDACH_REPORT_FLAT, FIRSTDATA_BATCH_FLAT,
                              FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON, FIRSTDATA_REPORTS_FLAT,
                              FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                              FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM,
                              NEUTRINO_JSON, OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON,
                              PAYRIX_ENCRYPTION_JSON, PAYRIX_GOOGLEPAY_DECRYPTION_JSON,
                              PAYRIX_TXN_JSON, PUSHY_TXN_JSON, SIFT_JSON, SOCURE_ALLOY_JSON,
                              SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM, SOUNDPAYMENTS_TXN_XML,
                              VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                              VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                              VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV,
                              VANTIV_TXN_XML, VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT,
                              VCORE_BINREPORT_FLAT, VCORE_BOARD_API, VCORE_BOARD_FLAT,
                              VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                              VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT,
                              VCORE_VARSHEET_JSON, WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT,
                              WELLSACH_PAYOUT_FLAT, WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML,
                              WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT, WFSINGLE_REPORT_FLAT,
                              WFSINGLE_BOARD_XML]
                            x-enum-descriptions: ['', '', '', '', '', '', '', '', '',
                              '', '', '', '', '', '', '', '', '', NACHA output file,
                              '', NEUTRINO_FORM output file, '', OFAC_XML output file,
                              '', '', '', '', '', '', SIFT_JSON output file, '', SOCURE_FORM
                                output file, SOCURE_JSON output file, SOUNDPAYMENTS_TXN_FORM
                                output file, SOUNDPAYMENTS_TXN_XML output file, VANTIV_BATCH_XML
                                output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                                output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                                output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                                output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT
                                output file, VCORE_BATCH_READ_FLAT output file, '',
                              '', VCORE_BOARD_FLAT output file, VCORE_CHARGEBACK_FLAT
                                output file, '', VCORE_REPORTS_FLAT output file, '',
                              VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT output
                                file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON
                                output file, '', '', '', '', '', '', '', '']
                          credential:
                            description: The credential used for configurationStage
                              resource.
                            enum: [transaction, batch, boarding, paymentUpdates, payout,
                              payoutCredit, payoutDebit, chargeback, txnReport, account,
                              entityDecision, txnCheck, payoutReport, feeReport, chargebackReport,
                              binReport, watchlist, encryption, changeRequest]
                            x-enum-descriptions: [Credential used for transaction processing,
                              Credential used for transaction batches, Credential used
                                merchant boarding, '', Credential used for payouts,
                              '', '', Credential used for chargeback processing and
                                updating, Credential used for transaction reports, Credential
                                used for account integrations, Credential used for entity
                                decisions, Credential used for transaction verifications,
                              Credential used for payout reports, Credential used for
                                fee reports, Credential used for chargeback reports,
                              Credential used binReports, '', '', '']
                          url:
                            description: |-
                              The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                              Valid placeholders are: entity, merchant, member, chargeback, and document.
                              The field is stored as a text string and must be between 1 and 500 characters long
                          dir:
                            description: "The directory used once connected. \nThis\
                              \ field is stored as a text string and must be between\
                              \ 1 and 500 characters long"
                          restId:
                            description: "The indicator of the field when ref Id is\
                              \ found. \nThis field is stored as a text string and must\
                              \ be between 0 and 100 characters long."
                          mapToData: {}
                          file:
                            description: "The specification on how the file will be\
                              \ named on write. \nThis field is stored as a JSON string\
                              \ and must be between 0 and 5000 characters long."
                          filter:
                            description: "The filter applied when successful on data\
                              \ read. \nThis field is stored as a JSON string and must\
                              \ be between 0 and 5000 characters long."
                          extension:
                            description: The extension of the file.
                            enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc,
                              rtf, csv, xls, doc, odt, ods, json, soap]
                            x-enum-descriptions: [jpg extension file type for the input
                                and output files, jpeg extension file type for the input
                                and output files, gif extension file type for the input
                                and output files, png extension file type for the input
                                and output files, pdf extension file type for the input
                                and output files, tif extension file type for the input
                                and output files, tiff extension file type for the input
                                and output files, txt extension file type for the input
                                and output files, xml extension file type for the input
                                and output files, asc extension file type for the input
                                and output files, rtf extension file type for the input
                                and output files, csv extension file type for the input
                                and output files, xls extension file type for the input
                                and output files, doc extension file type for the input
                                and output files, odt extension file type for the input
                                and output files, ods extension file type for the input
                                and output files, '', '']
                          namespace:
                            description: "The namespace used for the configurationStage\
                              \ resource. \nThis field is stored as a text string and\
                              \ must be between 0 and 500 characters long."
                          dynamicStage:
                            description: "Only used in case the it is dynamic stage.\
                              \ \nThis field is stored as a JSON string, must be between\
                              \ 0 and 5000 characters long, and have a property key\
                              \ name with value of stage name and id key."
                          curlOptions: {}
                          headers:
                            description: "The headers used for the configurationStage\
                              \ resource. \nThis field is stored as a JSON string and\
                              \ must be between 0 and 5000 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          configuration: {description: The identifier of the Configuration
                              that owns this configurationStage resource.}
                          name:
                            description: The name of this configurationStage resource.
                            enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate,
                              applepay, balance, batch, chargeback, chargebackList,
                              confirmDisbursement, contactUpdate, createMerchant, credit,
                              decrypt, debit, debitPinEnable, debitPinlessEnable, discoverUrl,
                              discoverUpdate, document, documentList, interchange, encrypt,
                              entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                              expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                              memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                              merchantCardNotPresent, merchantUpdate, merchantRetrieval,
                              noc, nocReturns, omniTokenEnable, passTokenEnable, payment,
                              paymentConfirmation, paymentUpdate, payout, primaryMemberUpdate,
                              registerSrs, returnCredit, returnDebit, returns, saferPaymentEnable,
                              terminalCreate, terminalList, terminalsSupported, txn,
                              txnList, varsheet]
                            x-enum-descriptions: [Blank. Used for a stage with no name,
                              '', '', '', '', '', '', '', '', Chargeback. The standard
                                name used for chargeback related stages, ChargebackList.
                                The specific name to get chargeback reports, '', '',
                              '', '', '', '', '', '', '', '', 'Document. The standard
                                name for chargeback related documentation such as uploading
                                a document or updating a document ', DocumentList. Used
                                for receiving documents related to a chargeback, '',
                              '', Entity. The standard name used when it comes to entity
                                related stages such as retrieving an entity or updating,
                              '', EntityRetrieval. Used when entity stage name is already
                                being used in that crud configuration, EntityUpdate.
                                Used when entity stage name is already being used in
                                that crud configuration, '', Return. Used for return
                                stage name, FeeList. The specific name to get fees reports,
                              '', MemberCreate. Stage name used to indicate creating
                                a merchant member, MemberDelete. Stage name used to
                                indicate deleting a merchant member, Members. Stage
                                name used for creating several members for the merchant,
                              '', Merchant. The standard name used for merchant related
                                stages such retrieving a merchant or updating a merchant,
                              '', '', '', MerchantRetrieval. Used when merchant stage
                                name is already being used in that crud configuration,
                              Noc. Stage name used for notification of change for disbursement
                                related integrations, '', '', '', '', '', '', Payout.
                                Stage name used for payouts, '', '', '', '', '', '',
                              '', '', '', Txn. The standard name used for transaction
                                related stages, TxnList. The specific name to get transaction
                                reports, '']
                          method:
                            description: The method used for configurationStage resource.
                            enum: [sftpUpload, sftpDownload, post, get, put, delete,
                              tcp]
                            x-enum-descriptions: [SFtpUpload. Will use sftp to upload,
                              '', Post. Will use a post request, Get. Will use a get
                                request, Put. Will use a put request, Delete. Will use
                                a delete request, Tcp. Will use tcp protocol]
                          sequence: {}
                          input:
                            description: The input used for configurationStage resource.
                            enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                              APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT,
                              ELAVON_BATCH_READ_FLAT, ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV,
                              ELAVON_TXN_XML, FEDACH_REPORT_FLAT, FIRSTDATA_BATCH_FLAT,
                              FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON, FIRSTDATA_REPORTS_FLAT,
                              FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                              FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM,
                              NEUTRINO_JSON, OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON,
                              PAYRIX_ENCRYPTION_JSON, PAYRIX_GOOGLEPAY_DECRYPTION_JSON,
                              PAYRIX_TXN_JSON, PUSHY_TXN_JSON, SIFT_JSON, SOCURE_ALLOY_JSON,
                              SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM, SOUNDPAYMENTS_TXN_XML,
                              VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                              VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                              VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV,
                              VANTIV_TXN_XML, VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT,
                              VCORE_BINREPORT_FLAT, VCORE_BOARD_API, VCORE_BOARD_FLAT,
                              VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                              VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT,
                              VCORE_VARSHEET_JSON, WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT,
                              WELLSACH_PAYOUT_FLAT, WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML,
                              WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT, WFSINGLE_REPORT_FLAT,
                              WFSINGLE_BOARD_XML]
                            x-enum-descriptions: ['', '', '', '', '', '', '', '', '',
                              '', '', '', '', '', '', '', '', '', NACHA input file,
                              '', NEUTRINO_FORM input file, '', OFAC_XML input file,
                              '', '', '', '', '', '', SIFT_JSON input file, '', SOCURE_FORM
                                input file, SOCURE_JSON input file, SOUNDPAYMENTS_TXN_FORM
                                input file, SOUNDPAYMENTS_TXN_XML input file, VANTIV_BATCH_XML
                                input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                                input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                                input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV
                                input file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT
                                input file, VCORE_BATCH_READ_FLAT input file, '', '',
                              VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT input
                                file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                                input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM
                                input file, WEBSHIELD_JSON input file, '', '', '', '',
                              '', '', '', '']
                          output:
                            description: The output used for configurationStage resource.
                            enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                              APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT,
                              ELAVON_BATCH_READ_FLAT, ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV,
                              ELAVON_TXN_XML, FEDACH_REPORT_FLAT, FIRSTDATA_BATCH_FLAT,
                              FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON, FIRSTDATA_REPORTS_FLAT,
                              FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                              FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM,
                              NEUTRINO_JSON, OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON,
                              PAYRIX_ENCRYPTION_JSON, PAYRIX_GOOGLEPAY_DECRYPTION_JSON,
                              PAYRIX_TXN_JSON, PUSHY_TXN_JSON, SIFT_JSON, SOCURE_ALLOY_JSON,
                              SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM, SOUNDPAYMENTS_TXN_XML,
                              VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                              VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                              VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV,
                              VANTIV_TXN_XML, VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT,
                              VCORE_BINREPORT_FLAT, VCORE_BOARD_API, VCORE_BOARD_FLAT,
                              VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                              VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT,
                              VCORE_VARSHEET_JSON, WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT,
                              WELLSACH_PAYOUT_FLAT, WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML,
                              WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT, WFSINGLE_REPORT_FLAT,
                              WFSINGLE_BOARD_XML]
                            x-enum-descriptions: ['', '', '', '', '', '', '', '', '',
                              '', '', '', '', '', '', '', '', '', NACHA output file,
                              '', NEUTRINO_FORM output file, '', OFAC_XML output file,
                              '', '', '', '', '', '', SIFT_JSON output file, '', SOCURE_FORM
                                output file, SOCURE_JSON output file, SOUNDPAYMENTS_TXN_FORM
                                output file, SOUNDPAYMENTS_TXN_XML output file, VANTIV_BATCH_XML
                                output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                                output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                                output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                                output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT
                                output file, VCORE_BATCH_READ_FLAT output file, '',
                              '', VCORE_BOARD_FLAT output file, VCORE_CHARGEBACK_FLAT
                                output file, '', VCORE_REPORTS_FLAT output file, '',
                              VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT output
                                file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON
                                output file, '', '', '', '', '', '', '', '']
                          credential:
                            description: The credential used for configurationStage
                              resource.
                            enum: [transaction, batch, boarding, paymentUpdates, payout,
                              payoutCredit, payoutDebit, chargeback, txnReport, account,
                              entityDecision, txnCheck, payoutReport, feeReport, chargebackReport,
                              binReport, watchlist, encryption, changeRequest]
                            x-enum-descriptions: [Credential used for transaction processing,
                              Credential used for transaction batches, Credential used
                                merchant boarding, '', Credential used for payouts,
                              '', '', Credential used for chargeback processing and
                                updating, Credential used for transaction reports, Credential
                                used for account integrations, Credential used for entity
                                decisions, Credential used for transaction verifications,
                              Credential used for payout reports, Credential used for
                                fee reports, Credential used for chargeback reports,
                              Credential used binReports, '', '', '']
                          url:
                            description: |-
                              The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                              Valid placeholders are: entity, merchant, member, chargeback, and document.
                              The field is stored as a text string and must be between 1 and 500 characters long
                          dir:
                            description: "The directory used once connected. \nThis\
                              \ field is stored as a text string and must be between\
                              \ 1 and 500 characters long"
                          restId:
                            description: "The indicator of the field when ref Id is\
                              \ found. \nThis field is stored as a text string and must\
                              \ be between 0 and 100 characters long."
                          mapToData: {}
                          file:
                            description: "The specification on how the file will be\
                              \ named on write. \nThis field is stored as a JSON string\
                              \ and must be between 0 and 5000 characters long."
                          filter:
                            description: "The filter applied when successful on data\
                              \ read. \nThis field is stored as a JSON string and must\
                              \ be between 0 and 5000 characters long."
                          extension:
                            description: The extension of the file.
                            enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc,
                              rtf, csv, xls, doc, odt, ods, json, soap]
                            x-enum-descriptions: [jpg extension file type for the input
                                and output files, jpeg extension file type for the input
                                and output files, gif extension file type for the input
                                and output files, png extension file type for the input
                                and output files, pdf extension file type for the input
                                and output files, tif extension file type for the input
                                and output files, tiff extension file type for the input
                                and output files, txt extension file type for the input
                                and output files, xml extension file type for the input
                                and output files, asc extension file type for the input
                                and output files, rtf extension file type for the input
                                and output files, csv extension file type for the input
                                and output files, xls extension file type for the input
                                and output files, doc extension file type for the input
                                and output files, odt extension file type for the input
                                and output files, ods extension file type for the input
                                and output files, '', '']
                          namespace:
                            description: "The namespace used for the configurationStage\
                              \ resource. \nThis field is stored as a text string and\
                              \ must be between 0 and 500 characters long."
                          dynamicStage:
                            description: "Only used in case the it is dynamic stage.\
                              \ \nThis field is stored as a JSON string, must be between\
                              \ 0 and 5000 characters long, and have a property key\
                              \ name with value of stage name and id key."
                          curlOptions: {}
                          headers:
                            description: "The headers used for the configurationStage\
                              \ resource. \nThis field is stored as a JSON string and\
                              \ must be between 0 and 5000 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: Response for query on configurationStages resource
    tags: [Configurations]
    operationId: getConfigurationStages
  post:
    description: "Create a ConfigurationStage resource. \nA ConfigurationStage resource\
      \ will hold relevant information about a certain stage of integration configuration."
    requestBody:
      content:
        application/json:
          schema:
            properties:
              configuration: {description: The identifier of the Configuration that
                  owns this configurationStage resource.}
              name:
                description: The name of this configurationStage resource.
                enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate, applepay,
                  balance, batch, chargeback, chargebackList, confirmDisbursement, contactUpdate,
                  createMerchant, credit, decrypt, debit, debitPinEnable, debitPinlessEnable,
                  discoverUrl, discoverUpdate, document, documentList, interchange,
                  encrypt, entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                  expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                  memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                  merchantCardNotPresent, merchantUpdate, merchantRetrieval, noc, nocReturns,
                  omniTokenEnable, passTokenEnable, payment, paymentConfirmation, paymentUpdate,
                  payout, primaryMemberUpdate, registerSrs, returnCredit, returnDebit,
                  returns, saferPaymentEnable, terminalCreate, terminalList, terminalsSupported,
                  txn, txnList, varsheet]
                x-enum-descriptions: [Blank. Used for a stage with no name, '', '',
                  '', '', '', '', '', '', Chargeback. The standard name used for chargeback
                    related stages, ChargebackList. The specific name to get chargeback
                    reports, '', '', '', '', '', '', '', '', '', '', 'Document. The
                    standard name for chargeback related documentation such as uploading
                    a document or updating a document ', DocumentList. Used for receiving
                    documents related to a chargeback, '', '', Entity. The standard
                    name used when it comes to entity related stages such as retrieving
                    an entity or updating, '', EntityRetrieval. Used when entity stage
                    name is already being used in that crud configuration, EntityUpdate.
                    Used when entity stage name is already being used in that crud configuration,
                  '', Return. Used for return stage name, FeeList. The specific name
                    to get fees reports, '', MemberCreate. Stage name used to indicate
                    creating a merchant member, MemberDelete. Stage name used to indicate
                    deleting a merchant member, Members. Stage name used for creating
                    several members for the merchant, '', Merchant. The standard name
                    used for merchant related stages such retrieving a merchant or updating
                    a merchant, '', '', '', MerchantRetrieval. Used when merchant stage
                    name is already being used in that crud configuration, Noc. Stage
                    name used for notification of change for disbursement related integrations,
                  '', '', '', '', '', '', Payout. Stage name used for payouts, '', '',
                  '', '', '', '', '', '', '', Txn. The standard name used for transaction
                    related stages, TxnList. The specific name to get transaction reports,
                  '']
              method:
                description: The method used for configurationStage resource.
                enum: [sftpUpload, sftpDownload, post, get, put, delete, tcp]
                x-enum-descriptions: [SFtpUpload. Will use sftp to upload, '', Post.
                    Will use a post request, Get. Will use a get request, Put. Will
                    use a put request, Delete. Will use a delete request, Tcp. Will
                    use tcp protocol]
              sequence: {}
              input:
                description: The input used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA input file, '', NEUTRINO_FORM input
                    file, '', OFAC_XML input file, '', '', '', '', '', '', SIFT_JSON
                    input file, '', SOCURE_FORM input file, SOCURE_JSON input file,
                  SOUNDPAYMENTS_TXN_FORM input file, SOUNDPAYMENTS_TXN_XML input file,
                  VANTIV_BATCH_XML input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                    input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                    input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV input
                    file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT input file, VCORE_BATCH_READ_FLAT
                    input file, '', '', VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT
                    input file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                    input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM input
                    file, WEBSHIELD_JSON input file, '', '', '', '', '', '', '', '']
              output:
                description: The output used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA output file, '', NEUTRINO_FORM output
                    file, '', OFAC_XML output file, '', '', '', '', '', '', SIFT_JSON
                    output file, '', SOCURE_FORM output file, SOCURE_JSON output file,
                  SOUNDPAYMENTS_TXN_FORM output file, SOUNDPAYMENTS_TXN_XML output file,
                  VANTIV_BATCH_XML output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                    output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                    output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                    output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT output
                    file, VCORE_BATCH_READ_FLAT output file, '', '', VCORE_BOARD_FLAT
                    output file, VCORE_CHARGEBACK_FLAT output file, '', VCORE_REPORTS_FLAT
                    output file, '', VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT
                    output file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON output
                    file, '', '', '', '', '', '', '', '']
              credential:
                description: The credential used for configurationStage resource.
                enum: [transaction, batch, boarding, paymentUpdates, payout, payoutCredit,
                  payoutDebit, chargeback, txnReport, account, entityDecision, txnCheck,
                  payoutReport, feeReport, chargebackReport, binReport, watchlist, encryption,
                  changeRequest]
                x-enum-descriptions: [Credential used for transaction processing, Credential
                    used for transaction batches, Credential used merchant boarding,
                  '', Credential used for payouts, '', '', Credential used for chargeback
                    processing and updating, Credential used for transaction reports,
                  Credential used for account integrations, Credential used for entity
                    decisions, Credential used for transaction verifications, Credential
                    used for payout reports, Credential used for fee reports, Credential
                    used for chargeback reports, Credential used binReports, '', '',
                  '']
              url:
                description: |-
                  The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                  Valid placeholders are: entity, merchant, member, chargeback, and document.
                  The field is stored as a text string and must be between 1 and 500 characters long
              dir:
                description: "The directory used once connected. \nThis field is stored\
                  \ as a text string and must be between 1 and 500 characters long"
              restId:
                description: "The indicator of the field when ref Id is found. \nThis\
                  \ field is stored as a text string and must be between 0 and 100 characters\
                  \ long."
              mapToData: {}
              file:
                description: "The specification on how the file will be named on write.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              filter:
                description: "The filter applied when successful on data read. \nThis\
                  \ field is stored as a JSON string and must be between 0 and 5000\
                  \ characters long."
              extension:
                description: The extension of the file.
                enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc, rtf, csv,
                  xls, doc, odt, ods, json, soap]
                x-enum-descriptions: [jpg extension file type for the input and output
                    files, jpeg extension file type for the input and output files,
                  gif extension file type for the input and output files, png extension
                    file type for the input and output files, pdf extension file type
                    for the input and output files, tif extension file type for the
                    input and output files, tiff extension file type for the input and
                    output files, txt extension file type for the input and output files,
                  xml extension file type for the input and output files, asc extension
                    file type for the input and output files, rtf extension file type
                    for the input and output files, csv extension file type for the
                    input and output files, xls extension file type for the input and
                    output files, doc extension file type for the input and output files,
                  odt extension file type for the input and output files, ods extension
                    file type for the input and output files, '', '']
              namespace:
                description: "The namespace used for the configurationStage resource.\
                  \ \nThis field is stored as a text string and must be between 0 and\
                  \ 500 characters long."
              dynamicStage:
                description: "Only used in case the it is dynamic stage. \nThis field\
                  \ is stored as a JSON string, must be between 0 and 5000 characters\
                  \ long, and have a property key name with value of stage name and\
                  \ id key."
              curlOptions: {}
              headers:
                description: "The headers used for the configurationStage resource.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [configuration, frozen, inactive, input, method, name, output,
              url]
            x-redacted: []
        application/x-www-form-urlencoded:
          schema:
            properties:
              configuration: {description: The identifier of the Configuration that
                  owns this configurationStage resource.}
              name:
                description: The name of this configurationStage resource.
                enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate, applepay,
                  balance, batch, chargeback, chargebackList, confirmDisbursement, contactUpdate,
                  createMerchant, credit, decrypt, debit, debitPinEnable, debitPinlessEnable,
                  discoverUrl, discoverUpdate, document, documentList, interchange,
                  encrypt, entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                  expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                  memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                  merchantCardNotPresent, merchantUpdate, merchantRetrieval, noc, nocReturns,
                  omniTokenEnable, passTokenEnable, payment, paymentConfirmation, paymentUpdate,
                  payout, primaryMemberUpdate, registerSrs, returnCredit, returnDebit,
                  returns, saferPaymentEnable, terminalCreate, terminalList, terminalsSupported,
                  txn, txnList, varsheet]
                x-enum-descriptions: [Blank. Used for a stage with no name, '', '',
                  '', '', '', '', '', '', Chargeback. The standard name used for chargeback
                    related stages, ChargebackList. The specific name to get chargeback
                    reports, '', '', '', '', '', '', '', '', '', '', 'Document. The
                    standard name for chargeback related documentation such as uploading
                    a document or updating a document ', DocumentList. Used for receiving
                    documents related to a chargeback, '', '', Entity. The standard
                    name used when it comes to entity related stages such as retrieving
                    an entity or updating, '', EntityRetrieval. Used when entity stage
                    name is already being used in that crud configuration, EntityUpdate.
                    Used when entity stage name is already being used in that crud configuration,
                  '', Return. Used for return stage name, FeeList. The specific name
                    to get fees reports, '', MemberCreate. Stage name used to indicate
                    creating a merchant member, MemberDelete. Stage name used to indicate
                    deleting a merchant member, Members. Stage name used for creating
                    several members for the merchant, '', Merchant. The standard name
                    used for merchant related stages such retrieving a merchant or updating
                    a merchant, '', '', '', MerchantRetrieval. Used when merchant stage
                    name is already being used in that crud configuration, Noc. Stage
                    name used for notification of change for disbursement related integrations,
                  '', '', '', '', '', '', Payout. Stage name used for payouts, '', '',
                  '', '', '', '', '', '', '', Txn. The standard name used for transaction
                    related stages, TxnList. The specific name to get transaction reports,
                  '']
              method:
                description: The method used for configurationStage resource.
                enum: [sftpUpload, sftpDownload, post, get, put, delete, tcp]
                x-enum-descriptions: [SFtpUpload. Will use sftp to upload, '', Post.
                    Will use a post request, Get. Will use a get request, Put. Will
                    use a put request, Delete. Will use a delete request, Tcp. Will
                    use tcp protocol]
              sequence: {}
              input:
                description: The input used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA input file, '', NEUTRINO_FORM input
                    file, '', OFAC_XML input file, '', '', '', '', '', '', SIFT_JSON
                    input file, '', SOCURE_FORM input file, SOCURE_JSON input file,
                  SOUNDPAYMENTS_TXN_FORM input file, SOUNDPAYMENTS_TXN_XML input file,
                  VANTIV_BATCH_XML input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                    input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                    input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV input
                    file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT input file, VCORE_BATCH_READ_FLAT
                    input file, '', '', VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT
                    input file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                    input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM input
                    file, WEBSHIELD_JSON input file, '', '', '', '', '', '', '', '']
              output:
                description: The output used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA output file, '', NEUTRINO_FORM output
                    file, '', OFAC_XML output file, '', '', '', '', '', '', SIFT_JSON
                    output file, '', SOCURE_FORM output file, SOCURE_JSON output file,
                  SOUNDPAYMENTS_TXN_FORM output file, SOUNDPAYMENTS_TXN_XML output file,
                  VANTIV_BATCH_XML output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                    output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                    output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                    output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT output
                    file, VCORE_BATCH_READ_FLAT output file, '', '', VCORE_BOARD_FLAT
                    output file, VCORE_CHARGEBACK_FLAT output file, '', VCORE_REPORTS_FLAT
                    output file, '', VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT
                    output file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON output
                    file, '', '', '', '', '', '', '', '']
              credential:
                description: The credential used for configurationStage resource.
                enum: [transaction, batch, boarding, paymentUpdates, payout, payoutCredit,
                  payoutDebit, chargeback, txnReport, account, entityDecision, txnCheck,
                  payoutReport, feeReport, chargebackReport, binReport, watchlist, encryption,
                  changeRequest]
                x-enum-descriptions: [Credential used for transaction processing, Credential
                    used for transaction batches, Credential used merchant boarding,
                  '', Credential used for payouts, '', '', Credential used for chargeback
                    processing and updating, Credential used for transaction reports,
                  Credential used for account integrations, Credential used for entity
                    decisions, Credential used for transaction verifications, Credential
                    used for payout reports, Credential used for fee reports, Credential
                    used for chargeback reports, Credential used binReports, '', '',
                  '']
              url:
                description: |-
                  The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                  Valid placeholders are: entity, merchant, member, chargeback, and document.
                  The field is stored as a text string and must be between 1 and 500 characters long
              dir:
                description: "The directory used once connected. \nThis field is stored\
                  \ as a text string and must be between 1 and 500 characters long"
              restId:
                description: "The indicator of the field when ref Id is found. \nThis\
                  \ field is stored as a text string and must be between 0 and 100 characters\
                  \ long."
              mapToData: {}
              file:
                description: "The specification on how the file will be named on write.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              filter:
                description: "The filter applied when successful on data read. \nThis\
                  \ field is stored as a JSON string and must be between 0 and 5000\
                  \ characters long."
              extension:
                description: The extension of the file.
                enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc, rtf, csv,
                  xls, doc, odt, ods, json, soap]
                x-enum-descriptions: [jpg extension file type for the input and output
                    files, jpeg extension file type for the input and output files,
                  gif extension file type for the input and output files, png extension
                    file type for the input and output files, pdf extension file type
                    for the input and output files, tif extension file type for the
                    input and output files, tiff extension file type for the input and
                    output files, txt extension file type for the input and output files,
                  xml extension file type for the input and output files, asc extension
                    file type for the input and output files, rtf extension file type
                    for the input and output files, csv extension file type for the
                    input and output files, xls extension file type for the input and
                    output files, doc extension file type for the input and output files,
                  odt extension file type for the input and output files, ods extension
                    file type for the input and output files, '', '']
              namespace:
                description: "The namespace used for the configurationStage resource.\
                  \ \nThis field is stored as a text string and must be between 0 and\
                  \ 500 characters long."
              dynamicStage:
                description: "Only used in case the it is dynamic stage. \nThis field\
                  \ is stored as a JSON string, must be between 0 and 5000 characters\
                  \ long, and have a property key name with value of stage name and\
                  \ id key."
              curlOptions: {}
              headers:
                description: "The headers used for the configurationStage resource.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [configuration, frozen, inactive, input, method, name, output,
              url]
            x-redacted: []
        application/xml:
          schema:
            properties:
              configuration: {description: The identifier of the Configuration that
                  owns this configurationStage resource.}
              name:
                description: The name of this configurationStage resource.
                enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate, applepay,
                  balance, batch, chargeback, chargebackList, confirmDisbursement, contactUpdate,
                  createMerchant, credit, decrypt, debit, debitPinEnable, debitPinlessEnable,
                  discoverUrl, discoverUpdate, document, documentList, interchange,
                  encrypt, entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                  expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                  memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                  merchantCardNotPresent, merchantUpdate, merchantRetrieval, noc, nocReturns,
                  omniTokenEnable, passTokenEnable, payment, paymentConfirmation, paymentUpdate,
                  payout, primaryMemberUpdate, registerSrs, returnCredit, returnDebit,
                  returns, saferPaymentEnable, terminalCreate, terminalList, terminalsSupported,
                  txn, txnList, varsheet]
                x-enum-descriptions: [Blank. Used for a stage with no name, '', '',
                  '', '', '', '', '', '', Chargeback. The standard name used for chargeback
                    related stages, ChargebackList. The specific name to get chargeback
                    reports, '', '', '', '', '', '', '', '', '', '', 'Document. The
                    standard name for chargeback related documentation such as uploading
                    a document or updating a document ', DocumentList. Used for receiving
                    documents related to a chargeback, '', '', Entity. The standard
                    name used when it comes to entity related stages such as retrieving
                    an entity or updating, '', EntityRetrieval. Used when entity stage
                    name is already being used in that crud configuration, EntityUpdate.
                    Used when entity stage name is already being used in that crud configuration,
                  '', Return. Used for return stage name, FeeList. The specific name
                    to get fees reports, '', MemberCreate. Stage name used to indicate
                    creating a merchant member, MemberDelete. Stage name used to indicate
                    deleting a merchant member, Members. Stage name used for creating
                    several members for the merchant, '', Merchant. The standard name
                    used for merchant related stages such retrieving a merchant or updating
                    a merchant, '', '', '', MerchantRetrieval. Used when merchant stage
                    name is already being used in that crud configuration, Noc. Stage
                    name used for notification of change for disbursement related integrations,
                  '', '', '', '', '', '', Payout. Stage name used for payouts, '', '',
                  '', '', '', '', '', '', '', Txn. The standard name used for transaction
                    related stages, TxnList. The specific name to get transaction reports,
                  '']
              method:
                description: The method used for configurationStage resource.
                enum: [sftpUpload, sftpDownload, post, get, put, delete, tcp]
                x-enum-descriptions: [SFtpUpload. Will use sftp to upload, '', Post.
                    Will use a post request, Get. Will use a get request, Put. Will
                    use a put request, Delete. Will use a delete request, Tcp. Will
                    use tcp protocol]
              sequence: {}
              input:
                description: The input used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA input file, '', NEUTRINO_FORM input
                    file, '', OFAC_XML input file, '', '', '', '', '', '', SIFT_JSON
                    input file, '', SOCURE_FORM input file, SOCURE_JSON input file,
                  SOUNDPAYMENTS_TXN_FORM input file, SOUNDPAYMENTS_TXN_XML input file,
                  VANTIV_BATCH_XML input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                    input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                    input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV input
                    file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT input file, VCORE_BATCH_READ_FLAT
                    input file, '', '', VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT
                    input file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                    input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM input
                    file, WEBSHIELD_JSON input file, '', '', '', '', '', '', '', '']
              output:
                description: The output used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA output file, '', NEUTRINO_FORM output
                    file, '', OFAC_XML output file, '', '', '', '', '', '', SIFT_JSON
                    output file, '', SOCURE_FORM output file, SOCURE_JSON output file,
                  SOUNDPAYMENTS_TXN_FORM output file, SOUNDPAYMENTS_TXN_XML output file,
                  VANTIV_BATCH_XML output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                    output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                    output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                    output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT output
                    file, VCORE_BATCH_READ_FLAT output file, '', '', VCORE_BOARD_FLAT
                    output file, VCORE_CHARGEBACK_FLAT output file, '', VCORE_REPORTS_FLAT
                    output file, '', VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT
                    output file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON output
                    file, '', '', '', '', '', '', '', '']
              credential:
                description: The credential used for configurationStage resource.
                enum: [transaction, batch, boarding, paymentUpdates, payout, payoutCredit,
                  payoutDebit, chargeback, txnReport, account, entityDecision, txnCheck,
                  payoutReport, feeReport, chargebackReport, binReport, watchlist, encryption,
                  changeRequest]
                x-enum-descriptions: [Credential used for transaction processing, Credential
                    used for transaction batches, Credential used merchant boarding,
                  '', Credential used for payouts, '', '', Credential used for chargeback
                    processing and updating, Credential used for transaction reports,
                  Credential used for account integrations, Credential used for entity
                    decisions, Credential used for transaction verifications, Credential
                    used for payout reports, Credential used for fee reports, Credential
                    used for chargeback reports, Credential used binReports, '', '',
                  '']
              url:
                description: |-
                  The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                  Valid placeholders are: entity, merchant, member, chargeback, and document.
                  The field is stored as a text string and must be between 1 and 500 characters long
              dir:
                description: "The directory used once connected. \nThis field is stored\
                  \ as a text string and must be between 1 and 500 characters long"
              restId:
                description: "The indicator of the field when ref Id is found. \nThis\
                  \ field is stored as a text string and must be between 0 and 100 characters\
                  \ long."
              mapToData: {}
              file:
                description: "The specification on how the file will be named on write.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              filter:
                description: "The filter applied when successful on data read. \nThis\
                  \ field is stored as a JSON string and must be between 0 and 5000\
                  \ characters long."
              extension:
                description: The extension of the file.
                enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc, rtf, csv,
                  xls, doc, odt, ods, json, soap]
                x-enum-descriptions: [jpg extension file type for the input and output
                    files, jpeg extension file type for the input and output files,
                  gif extension file type for the input and output files, png extension
                    file type for the input and output files, pdf extension file type
                    for the input and output files, tif extension file type for the
                    input and output files, tiff extension file type for the input and
                    output files, txt extension file type for the input and output files,
                  xml extension file type for the input and output files, asc extension
                    file type for the input and output files, rtf extension file type
                    for the input and output files, csv extension file type for the
                    input and output files, xls extension file type for the input and
                    output files, doc extension file type for the input and output files,
                  odt extension file type for the input and output files, ods extension
                    file type for the input and output files, '', '']
              namespace:
                description: "The namespace used for the configurationStage resource.\
                  \ \nThis field is stored as a text string and must be between 0 and\
                  \ 500 characters long."
              dynamicStage:
                description: "Only used in case the it is dynamic stage. \nThis field\
                  \ is stored as a JSON string, must be between 0 and 5000 characters\
                  \ long, and have a property key name with value of stage name and\
                  \ id key."
              curlOptions: {}
              headers:
                description: "The headers used for the configurationStage resource.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [configuration, frozen, inactive, input, method, name, output,
              url]
            x-redacted: []
        multipart/form-data:
          schema:
            properties:
              configuration: {description: The identifier of the Configuration that
                  owns this configurationStage resource.}
              name:
                description: The name of this configurationStage resource.
                enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate, applepay,
                  balance, batch, chargeback, chargebackList, confirmDisbursement, contactUpdate,
                  createMerchant, credit, decrypt, debit, debitPinEnable, debitPinlessEnable,
                  discoverUrl, discoverUpdate, document, documentList, interchange,
                  encrypt, entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                  expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                  memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                  merchantCardNotPresent, merchantUpdate, merchantRetrieval, noc, nocReturns,
                  omniTokenEnable, passTokenEnable, payment, paymentConfirmation, paymentUpdate,
                  payout, primaryMemberUpdate, registerSrs, returnCredit, returnDebit,
                  returns, saferPaymentEnable, terminalCreate, terminalList, terminalsSupported,
                  txn, txnList, varsheet]
                x-enum-descriptions: [Blank. Used for a stage with no name, '', '',
                  '', '', '', '', '', '', Chargeback. The standard name used for chargeback
                    related stages, ChargebackList. The specific name to get chargeback
                    reports, '', '', '', '', '', '', '', '', '', '', 'Document. The
                    standard name for chargeback related documentation such as uploading
                    a document or updating a document ', DocumentList. Used for receiving
                    documents related to a chargeback, '', '', Entity. The standard
                    name used when it comes to entity related stages such as retrieving
                    an entity or updating, '', EntityRetrieval. Used when entity stage
                    name is already being used in that crud configuration, EntityUpdate.
                    Used when entity stage name is already being used in that crud configuration,
                  '', Return. Used for return stage name, FeeList. The specific name
                    to get fees reports, '', MemberCreate. Stage name used to indicate
                    creating a merchant member, MemberDelete. Stage name used to indicate
                    deleting a merchant member, Members. Stage name used for creating
                    several members for the merchant, '', Merchant. The standard name
                    used for merchant related stages such retrieving a merchant or updating
                    a merchant, '', '', '', MerchantRetrieval. Used when merchant stage
                    name is already being used in that crud configuration, Noc. Stage
                    name used for notification of change for disbursement related integrations,
                  '', '', '', '', '', '', Payout. Stage name used for payouts, '', '',
                  '', '', '', '', '', '', '', Txn. The standard name used for transaction
                    related stages, TxnList. The specific name to get transaction reports,
                  '']
              method:
                description: The method used for configurationStage resource.
                enum: [sftpUpload, sftpDownload, post, get, put, delete, tcp]
                x-enum-descriptions: [SFtpUpload. Will use sftp to upload, '', Post.
                    Will use a post request, Get. Will use a get request, Put. Will
                    use a put request, Delete. Will use a delete request, Tcp. Will
                    use tcp protocol]
              sequence: {}
              input:
                description: The input used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA input file, '', NEUTRINO_FORM input
                    file, '', OFAC_XML input file, '', '', '', '', '', '', SIFT_JSON
                    input file, '', SOCURE_FORM input file, SOCURE_JSON input file,
                  SOUNDPAYMENTS_TXN_FORM input file, SOUNDPAYMENTS_TXN_XML input file,
                  VANTIV_BATCH_XML input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                    input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                    input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV input
                    file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT input file, VCORE_BATCH_READ_FLAT
                    input file, '', '', VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT
                    input file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                    input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM input
                    file, WEBSHIELD_JSON input file, '', '', '', '', '', '', '', '']
              output:
                description: The output used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA output file, '', NEUTRINO_FORM output
                    file, '', OFAC_XML output file, '', '', '', '', '', '', SIFT_JSON
                    output file, '', SOCURE_FORM output file, SOCURE_JSON output file,
                  SOUNDPAYMENTS_TXN_FORM output file, SOUNDPAYMENTS_TXN_XML output file,
                  VANTIV_BATCH_XML output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                    output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                    output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                    output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT output
                    file, VCORE_BATCH_READ_FLAT output file, '', '', VCORE_BOARD_FLAT
                    output file, VCORE_CHARGEBACK_FLAT output file, '', VCORE_REPORTS_FLAT
                    output file, '', VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT
                    output file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON output
                    file, '', '', '', '', '', '', '', '']
              credential:
                description: The credential used for configurationStage resource.
                enum: [transaction, batch, boarding, paymentUpdates, payout, payoutCredit,
                  payoutDebit, chargeback, txnReport, account, entityDecision, txnCheck,
                  payoutReport, feeReport, chargebackReport, binReport, watchlist, encryption,
                  changeRequest]
                x-enum-descriptions: [Credential used for transaction processing, Credential
                    used for transaction batches, Credential used merchant boarding,
                  '', Credential used for payouts, '', '', Credential used for chargeback
                    processing and updating, Credential used for transaction reports,
                  Credential used for account integrations, Credential used for entity
                    decisions, Credential used for transaction verifications, Credential
                    used for payout reports, Credential used for fee reports, Credential
                    used for chargeback reports, Credential used binReports, '', '',
                  '']
              url:
                description: |-
                  The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                  Valid placeholders are: entity, merchant, member, chargeback, and document.
                  The field is stored as a text string and must be between 1 and 500 characters long
              dir:
                description: "The directory used once connected. \nThis field is stored\
                  \ as a text string and must be between 1 and 500 characters long"
              restId:
                description: "The indicator of the field when ref Id is found. \nThis\
                  \ field is stored as a text string and must be between 0 and 100 characters\
                  \ long."
              mapToData: {}
              file:
                description: "The specification on how the file will be named on write.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              filter:
                description: "The filter applied when successful on data read. \nThis\
                  \ field is stored as a JSON string and must be between 0 and 5000\
                  \ characters long."
              extension:
                description: The extension of the file.
                enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc, rtf, csv,
                  xls, doc, odt, ods, json, soap]
                x-enum-descriptions: [jpg extension file type for the input and output
                    files, jpeg extension file type for the input and output files,
                  gif extension file type for the input and output files, png extension
                    file type for the input and output files, pdf extension file type
                    for the input and output files, tif extension file type for the
                    input and output files, tiff extension file type for the input and
                    output files, txt extension file type for the input and output files,
                  xml extension file type for the input and output files, asc extension
                    file type for the input and output files, rtf extension file type
                    for the input and output files, csv extension file type for the
                    input and output files, xls extension file type for the input and
                    output files, doc extension file type for the input and output files,
                  odt extension file type for the input and output files, ods extension
                    file type for the input and output files, '', '']
              namespace:
                description: "The namespace used for the configurationStage resource.\
                  \ \nThis field is stored as a text string and must be between 0 and\
                  \ 500 characters long."
              dynamicStage:
                description: "Only used in case the it is dynamic stage. \nThis field\
                  \ is stored as a JSON string, must be between 0 and 5000 characters\
                  \ long, and have a property key name with value of stage name and\
                  \ id key."
              curlOptions: {}
              headers:
                description: "The headers used for the configurationStage resource.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [configuration, frozen, inactive, input, method, name, output,
              url]
            x-redacted: []
        text/json:
          schema:
            properties:
              configuration: {description: The identifier of the Configuration that
                  owns this configurationStage resource.}
              name:
                description: The name of this configurationStage resource.
                enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate, applepay,
                  balance, batch, chargeback, chargebackList, confirmDisbursement, contactUpdate,
                  createMerchant, credit, decrypt, debit, debitPinEnable, debitPinlessEnable,
                  discoverUrl, discoverUpdate, document, documentList, interchange,
                  encrypt, entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                  expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                  memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                  merchantCardNotPresent, merchantUpdate, merchantRetrieval, noc, nocReturns,
                  omniTokenEnable, passTokenEnable, payment, paymentConfirmation, paymentUpdate,
                  payout, primaryMemberUpdate, registerSrs, returnCredit, returnDebit,
                  returns, saferPaymentEnable, terminalCreate, terminalList, terminalsSupported,
                  txn, txnList, varsheet]
                x-enum-descriptions: [Blank. Used for a stage with no name, '', '',
                  '', '', '', '', '', '', Chargeback. The standard name used for chargeback
                    related stages, ChargebackList. The specific name to get chargeback
                    reports, '', '', '', '', '', '', '', '', '', '', 'Document. The
                    standard name for chargeback related documentation such as uploading
                    a document or updating a document ', DocumentList. Used for receiving
                    documents related to a chargeback, '', '', Entity. The standard
                    name used when it comes to entity related stages such as retrieving
                    an entity or updating, '', EntityRetrieval. Used when entity stage
                    name is already being used in that crud configuration, EntityUpdate.
                    Used when entity stage name is already being used in that crud configuration,
                  '', Return. Used for return stage name, FeeList. The specific name
                    to get fees reports, '', MemberCreate. Stage name used to indicate
                    creating a merchant member, MemberDelete. Stage name used to indicate
                    deleting a merchant member, Members. Stage name used for creating
                    several members for the merchant, '', Merchant. The standard name
                    used for merchant related stages such retrieving a merchant or updating
                    a merchant, '', '', '', MerchantRetrieval. Used when merchant stage
                    name is already being used in that crud configuration, Noc. Stage
                    name used for notification of change for disbursement related integrations,
                  '', '', '', '', '', '', Payout. Stage name used for payouts, '', '',
                  '', '', '', '', '', '', '', Txn. The standard name used for transaction
                    related stages, TxnList. The specific name to get transaction reports,
                  '']
              method:
                description: The method used for configurationStage resource.
                enum: [sftpUpload, sftpDownload, post, get, put, delete, tcp]
                x-enum-descriptions: [SFtpUpload. Will use sftp to upload, '', Post.
                    Will use a post request, Get. Will use a get request, Put. Will
                    use a put request, Delete. Will use a delete request, Tcp. Will
                    use tcp protocol]
              sequence: {}
              input:
                description: The input used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA input file, '', NEUTRINO_FORM input
                    file, '', OFAC_XML input file, '', '', '', '', '', '', SIFT_JSON
                    input file, '', SOCURE_FORM input file, SOCURE_JSON input file,
                  SOUNDPAYMENTS_TXN_FORM input file, SOUNDPAYMENTS_TXN_XML input file,
                  VANTIV_BATCH_XML input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                    input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                    input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV input
                    file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT input file, VCORE_BATCH_READ_FLAT
                    input file, '', '', VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT
                    input file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                    input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM input
                    file, WEBSHIELD_JSON input file, '', '', '', '', '', '', '', '']
              output:
                description: The output used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA output file, '', NEUTRINO_FORM output
                    file, '', OFAC_XML output file, '', '', '', '', '', '', SIFT_JSON
                    output file, '', SOCURE_FORM output file, SOCURE_JSON output file,
                  SOUNDPAYMENTS_TXN_FORM output file, SOUNDPAYMENTS_TXN_XML output file,
                  VANTIV_BATCH_XML output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                    output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                    output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                    output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT output
                    file, VCORE_BATCH_READ_FLAT output file, '', '', VCORE_BOARD_FLAT
                    output file, VCORE_CHARGEBACK_FLAT output file, '', VCORE_REPORTS_FLAT
                    output file, '', VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT
                    output file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON output
                    file, '', '', '', '', '', '', '', '']
              credential:
                description: The credential used for configurationStage resource.
                enum: [transaction, batch, boarding, paymentUpdates, payout, payoutCredit,
                  payoutDebit, chargeback, txnReport, account, entityDecision, txnCheck,
                  payoutReport, feeReport, chargebackReport, binReport, watchlist, encryption,
                  changeRequest]
                x-enum-descriptions: [Credential used for transaction processing, Credential
                    used for transaction batches, Credential used merchant boarding,
                  '', Credential used for payouts, '', '', Credential used for chargeback
                    processing and updating, Credential used for transaction reports,
                  Credential used for account integrations, Credential used for entity
                    decisions, Credential used for transaction verifications, Credential
                    used for payout reports, Credential used for fee reports, Credential
                    used for chargeback reports, Credential used binReports, '', '',
                  '']
              url:
                description: |-
                  The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                  Valid placeholders are: entity, merchant, member, chargeback, and document.
                  The field is stored as a text string and must be between 1 and 500 characters long
              dir:
                description: "The directory used once connected. \nThis field is stored\
                  \ as a text string and must be between 1 and 500 characters long"
              restId:
                description: "The indicator of the field when ref Id is found. \nThis\
                  \ field is stored as a text string and must be between 0 and 100 characters\
                  \ long."
              mapToData: {}
              file:
                description: "The specification on how the file will be named on write.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              filter:
                description: "The filter applied when successful on data read. \nThis\
                  \ field is stored as a JSON string and must be between 0 and 5000\
                  \ characters long."
              extension:
                description: The extension of the file.
                enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc, rtf, csv,
                  xls, doc, odt, ods, json, soap]
                x-enum-descriptions: [jpg extension file type for the input and output
                    files, jpeg extension file type for the input and output files,
                  gif extension file type for the input and output files, png extension
                    file type for the input and output files, pdf extension file type
                    for the input and output files, tif extension file type for the
                    input and output files, tiff extension file type for the input and
                    output files, txt extension file type for the input and output files,
                  xml extension file type for the input and output files, asc extension
                    file type for the input and output files, rtf extension file type
                    for the input and output files, csv extension file type for the
                    input and output files, xls extension file type for the input and
                    output files, doc extension file type for the input and output files,
                  odt extension file type for the input and output files, ods extension
                    file type for the input and output files, '', '']
              namespace:
                description: "The namespace used for the configurationStage resource.\
                  \ \nThis field is stored as a text string and must be between 0 and\
                  \ 500 characters long."
              dynamicStage:
                description: "Only used in case the it is dynamic stage. \nThis field\
                  \ is stored as a JSON string, must be between 0 and 5000 characters\
                  \ long, and have a property key name with value of stage name and\
                  \ id key."
              curlOptions: {}
              headers:
                description: "The headers used for the configurationStage resource.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [configuration, frozen, inactive, input, method, name, output,
              url]
            x-redacted: []
        text/xml:
          schema:
            properties:
              configuration: {description: The identifier of the Configuration that
                  owns this configurationStage resource.}
              name:
                description: The name of this configurationStage resource.
                enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate, applepay,
                  balance, batch, chargeback, chargebackList, confirmDisbursement, contactUpdate,
                  createMerchant, credit, decrypt, debit, debitPinEnable, debitPinlessEnable,
                  discoverUrl, discoverUpdate, document, documentList, interchange,
                  encrypt, entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                  expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                  memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                  merchantCardNotPresent, merchantUpdate, merchantRetrieval, noc, nocReturns,
                  omniTokenEnable, passTokenEnable, payment, paymentConfirmation, paymentUpdate,
                  payout, primaryMemberUpdate, registerSrs, returnCredit, returnDebit,
                  returns, saferPaymentEnable, terminalCreate, terminalList, terminalsSupported,
                  txn, txnList, varsheet]
                x-enum-descriptions: [Blank. Used for a stage with no name, '', '',
                  '', '', '', '', '', '', Chargeback. The standard name used for chargeback
                    related stages, ChargebackList. The specific name to get chargeback
                    reports, '', '', '', '', '', '', '', '', '', '', 'Document. The
                    standard name for chargeback related documentation such as uploading
                    a document or updating a document ', DocumentList. Used for receiving
                    documents related to a chargeback, '', '', Entity. The standard
                    name used when it comes to entity related stages such as retrieving
                    an entity or updating, '', EntityRetrieval. Used when entity stage
                    name is already being used in that crud configuration, EntityUpdate.
                    Used when entity stage name is already being used in that crud configuration,
                  '', Return. Used for return stage name, FeeList. The specific name
                    to get fees reports, '', MemberCreate. Stage name used to indicate
                    creating a merchant member, MemberDelete. Stage name used to indicate
                    deleting a merchant member, Members. Stage name used for creating
                    several members for the merchant, '', Merchant. The standard name
                    used for merchant related stages such retrieving a merchant or updating
                    a merchant, '', '', '', MerchantRetrieval. Used when merchant stage
                    name is already being used in that crud configuration, Noc. Stage
                    name used for notification of change for disbursement related integrations,
                  '', '', '', '', '', '', Payout. Stage name used for payouts, '', '',
                  '', '', '', '', '', '', '', Txn. The standard name used for transaction
                    related stages, TxnList. The specific name to get transaction reports,
                  '']
              method:
                description: The method used for configurationStage resource.
                enum: [sftpUpload, sftpDownload, post, get, put, delete, tcp]
                x-enum-descriptions: [SFtpUpload. Will use sftp to upload, '', Post.
                    Will use a post request, Get. Will use a get request, Put. Will
                    use a put request, Delete. Will use a delete request, Tcp. Will
                    use tcp protocol]
              sequence: {}
              input:
                description: The input used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA input file, '', NEUTRINO_FORM input
                    file, '', OFAC_XML input file, '', '', '', '', '', '', SIFT_JSON
                    input file, '', SOCURE_FORM input file, SOCURE_JSON input file,
                  SOUNDPAYMENTS_TXN_FORM input file, SOUNDPAYMENTS_TXN_XML input file,
                  VANTIV_BATCH_XML input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                    input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                    input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV input
                    file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT input file, VCORE_BATCH_READ_FLAT
                    input file, '', '', VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT
                    input file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                    input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM input
                    file, WEBSHIELD_JSON input file, '', '', '', '', '', '', '', '']
              output:
                description: The output used for configurationStage resource.
                enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                  APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT, ELAVON_BATCH_READ_FLAT,
                  ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV, ELAVON_TXN_XML, FEDACH_REPORT_FLAT,
                  FIRSTDATA_BATCH_FLAT, FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON,
                  FIRSTDATA_REPORTS_FLAT, FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                  FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM, NEUTRINO_JSON,
                  OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON, PAYRIX_ENCRYPTION_JSON,
                  PAYRIX_GOOGLEPAY_DECRYPTION_JSON, PAYRIX_TXN_JSON, PUSHY_TXN_JSON,
                  SIFT_JSON, SOCURE_ALLOY_JSON, SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM,
                  SOUNDPAYMENTS_TXN_XML, VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                  VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                  VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV, VANTIV_TXN_XML,
                  VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT, VCORE_BINREPORT_FLAT, VCORE_BOARD_API,
                  VCORE_BOARD_FLAT, VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                  VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT, VCORE_VARSHEET_JSON,
                  WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT, WELLSACH_PAYOUT_FLAT,
                  WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML, WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT,
                  WFSINGLE_REPORT_FLAT, WFSINGLE_BOARD_XML]
                x-enum-descriptions: ['', '', '', '', '', '', '', '', '', '', '', '',
                  '', '', '', '', '', '', NACHA output file, '', NEUTRINO_FORM output
                    file, '', OFAC_XML output file, '', '', '', '', '', '', SIFT_JSON
                    output file, '', SOCURE_FORM output file, SOCURE_JSON output file,
                  SOUNDPAYMENTS_TXN_FORM output file, SOUNDPAYMENTS_TXN_XML output file,
                  VANTIV_BATCH_XML output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                    output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                    output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                    output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT output
                    file, VCORE_BATCH_READ_FLAT output file, '', '', VCORE_BOARD_FLAT
                    output file, VCORE_CHARGEBACK_FLAT output file, '', VCORE_REPORTS_FLAT
                    output file, '', VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT
                    output file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON output
                    file, '', '', '', '', '', '', '', '']
              credential:
                description: The credential used for configurationStage resource.
                enum: [transaction, batch, boarding, paymentUpdates, payout, payoutCredit,
                  payoutDebit, chargeback, txnReport, account, entityDecision, txnCheck,
                  payoutReport, feeReport, chargebackReport, binReport, watchlist, encryption,
                  changeRequest]
                x-enum-descriptions: [Credential used for transaction processing, Credential
                    used for transaction batches, Credential used merchant boarding,
                  '', Credential used for payouts, '', '', Credential used for chargeback
                    processing and updating, Credential used for transaction reports,
                  Credential used for account integrations, Credential used for entity
                    decisions, Credential used for transaction verifications, Credential
                    used for payout reports, Credential used for fee reports, Credential
                    used for chargeback reports, Credential used binReports, '', '',
                  '']
              url:
                description: |-
                  The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                  Valid placeholders are: entity, merchant, member, chargeback, and document.
                  The field is stored as a text string and must be between 1 and 500 characters long
              dir:
                description: "The directory used once connected. \nThis field is stored\
                  \ as a text string and must be between 1 and 500 characters long"
              restId:
                description: "The indicator of the field when ref Id is found. \nThis\
                  \ field is stored as a text string and must be between 0 and 100 characters\
                  \ long."
              mapToData: {}
              file:
                description: "The specification on how the file will be named on write.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              filter:
                description: "The filter applied when successful on data read. \nThis\
                  \ field is stored as a JSON string and must be between 0 and 5000\
                  \ characters long."
              extension:
                description: The extension of the file.
                enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc, rtf, csv,
                  xls, doc, odt, ods, json, soap]
                x-enum-descriptions: [jpg extension file type for the input and output
                    files, jpeg extension file type for the input and output files,
                  gif extension file type for the input and output files, png extension
                    file type for the input and output files, pdf extension file type
                    for the input and output files, tif extension file type for the
                    input and output files, tiff extension file type for the input and
                    output files, txt extension file type for the input and output files,
                  xml extension file type for the input and output files, asc extension
                    file type for the input and output files, rtf extension file type
                    for the input and output files, csv extension file type for the
                    input and output files, xls extension file type for the input and
                    output files, doc extension file type for the input and output files,
                  odt extension file type for the input and output files, ods extension
                    file type for the input and output files, '', '']
              namespace:
                description: "The namespace used for the configurationStage resource.\
                  \ \nThis field is stored as a text string and must be between 0 and\
                  \ 500 characters long."
              dynamicStage:
                description: "Only used in case the it is dynamic stage. \nThis field\
                  \ is stored as a JSON string, must be between 0 and 5000 characters\
                  \ long, and have a property key name with value of stage name and\
                  \ id key."
              curlOptions: {}
              headers:
                description: "The headers used for the configurationStage resource.\
                  \ \nThis field is stored as a JSON string and must be between 0 and\
                  \ 5000 characters long."
              inactive:
                description: Whether this resource is marked as inactive.
                enum: ['0', '1']
                x-enum-descriptions: [Active, Inactive]
              frozen:
                description: Whether this resource is marked as frozen.
                enum: ['0', '1']
                x-enum-descriptions: [Not frozen, Frozen]
            required: [configuration, frozen, inactive, input, method, name, output,
              url]
            x-redacted: []
      required: true
    responses:
      '200':
        content:
          application/json:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          configuration: {description: The identifier of the Configuration
                              that owns this configurationStage resource.}
                          name:
                            description: The name of this configurationStage resource.
                            enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate,
                              applepay, balance, batch, chargeback, chargebackList,
                              confirmDisbursement, contactUpdate, createMerchant, credit,
                              decrypt, debit, debitPinEnable, debitPinlessEnable, discoverUrl,
                              discoverUpdate, document, documentList, interchange, encrypt,
                              entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                              expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                              memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                              merchantCardNotPresent, merchantUpdate, merchantRetrieval,
                              noc, nocReturns, omniTokenEnable, passTokenEnable, payment,
                              paymentConfirmation, paymentUpdate, payout, primaryMemberUpdate,
                              registerSrs, returnCredit, returnDebit, returns, saferPaymentEnable,
                              terminalCreate, terminalList, terminalsSupported, txn,
                              txnList, varsheet]
                            x-enum-descriptions: [Blank. Used for a stage with no name,
                              '', '', '', '', '', '', '', '', Chargeback. The standard
                                name used for chargeback related stages, ChargebackList.
                                The specific name to get chargeback reports, '', '',
                              '', '', '', '', '', '', '', '', 'Document. The standard
                                name for chargeback related documentation such as uploading
                                a document or updating a document ', DocumentList. Used
                                for receiving documents related to a chargeback, '',
                              '', Entity. The standard name used when it comes to entity
                                related stages such as retrieving an entity or updating,
                              '', EntityRetrieval. Used when entity stage name is already
                                being used in that crud configuration, EntityUpdate.
                                Used when entity stage name is already being used in
                                that crud configuration, '', Return. Used for return
                                stage name, FeeList. The specific name to get fees reports,
                              '', MemberCreate. Stage name used to indicate creating
                                a merchant member, MemberDelete. Stage name used to
                                indicate deleting a merchant member, Members. Stage
                                name used for creating several members for the merchant,
                              '', Merchant. The standard name used for merchant related
                                stages such retrieving a merchant or updating a merchant,
                              '', '', '', MerchantRetrieval. Used when merchant stage
                                name is already being used in that crud configuration,
                              Noc. Stage name used for notification of change for disbursement
                                related integrations, '', '', '', '', '', '', Payout.
                                Stage name used for payouts, '', '', '', '', '', '',
                              '', '', '', Txn. The standard name used for transaction
                                related stages, TxnList. The specific name to get transaction
                                reports, '']
                          method:
                            description: The method used for configurationStage resource.
                            enum: [sftpUpload, sftpDownload, post, get, put, delete,
                              tcp]
                            x-enum-descriptions: [SFtpUpload. Will use sftp to upload,
                              '', Post. Will use a post request, Get. Will use a get
                                request, Put. Will use a put request, Delete. Will use
                                a delete request, Tcp. Will use tcp protocol]
                          sequence: {}
                          input:
                            description: The input used for configurationStage resource.
                            enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                              APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT,
                              ELAVON_BATCH_READ_FLAT, ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV,
                              ELAVON_TXN_XML, FEDACH_REPORT_FLAT, FIRSTDATA_BATCH_FLAT,
                              FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON, FIRSTDATA_REPORTS_FLAT,
                              FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                              FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM,
                              NEUTRINO_JSON, OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON,
                              PAYRIX_ENCRYPTION_JSON, PAYRIX_GOOGLEPAY_DECRYPTION_JSON,
                              PAYRIX_TXN_JSON, PUSHY_TXN_JSON, SIFT_JSON, SOCURE_ALLOY_JSON,
                              SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM, SOUNDPAYMENTS_TXN_XML,
                              VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                              VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                              VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV,
                              VANTIV_TXN_XML, VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT,
                              VCORE_BINREPORT_FLAT, VCORE_BOARD_API, VCORE_BOARD_FLAT,
                              VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                              VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT,
                              VCORE_VARSHEET_JSON, WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT,
                              WELLSACH_PAYOUT_FLAT, WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML,
                              WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT, WFSINGLE_REPORT_FLAT,
                              WFSINGLE_BOARD_XML]
                            x-enum-descriptions: ['', '', '', '', '', '', '', '', '',
                              '', '', '', '', '', '', '', '', '', NACHA input file,
                              '', NEUTRINO_FORM input file, '', OFAC_XML input file,
                              '', '', '', '', '', '', SIFT_JSON input file, '', SOCURE_FORM
                                input file, SOCURE_JSON input file, SOUNDPAYMENTS_TXN_FORM
                                input file, SOUNDPAYMENTS_TXN_XML input file, VANTIV_BATCH_XML
                                input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                                input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                                input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV
                                input file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT
                                input file, VCORE_BATCH_READ_FLAT input file, '', '',
                              VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT input
                                file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                                input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM
                                input file, WEBSHIELD_JSON input file, '', '', '', '',
                              '', '', '', '']
                          output:
                            description: The output used for configurationStage resource.
                            enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                              APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT,
                              ELAVON_BATCH_READ_FLAT, ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV,
                              ELAVON_TXN_XML, FEDACH_REPORT_FLAT, FIRSTDATA_BATCH_FLAT,
                              FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON, FIRSTDATA_REPORTS_FLAT,
                              FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                              FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM,
                              NEUTRINO_JSON, OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON,
                              PAYRIX_ENCRYPTION_JSON, PAYRIX_GOOGLEPAY_DECRYPTION_JSON,
                              PAYRIX_TXN_JSON, PUSHY_TXN_JSON, SIFT_JSON, SOCURE_ALLOY_JSON,
                              SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM, SOUNDPAYMENTS_TXN_XML,
                              VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                              VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                              VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV,
                              VANTIV_TXN_XML, VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT,
                              VCORE_BINREPORT_FLAT, VCORE_BOARD_API, VCORE_BOARD_FLAT,
                              VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                              VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT,
                              VCORE_VARSHEET_JSON, WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT,
                              WELLSACH_PAYOUT_FLAT, WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML,
                              WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT, WFSINGLE_REPORT_FLAT,
                              WFSINGLE_BOARD_XML]
                            x-enum-descriptions: ['', '', '', '', '', '', '', '', '',
                              '', '', '', '', '', '', '', '', '', NACHA output file,
                              '', NEUTRINO_FORM output file, '', OFAC_XML output file,
                              '', '', '', '', '', '', SIFT_JSON output file, '', SOCURE_FORM
                                output file, SOCURE_JSON output file, SOUNDPAYMENTS_TXN_FORM
                                output file, SOUNDPAYMENTS_TXN_XML output file, VANTIV_BATCH_XML
                                output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                                output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                                output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                                output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT
                                output file, VCORE_BATCH_READ_FLAT output file, '',
                              '', VCORE_BOARD_FLAT output file, VCORE_CHARGEBACK_FLAT
                                output file, '', VCORE_REPORTS_FLAT output file, '',
                              VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT output
                                file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON
                                output file, '', '', '', '', '', '', '', '']
                          credential:
                            description: The credential used for configurationStage
                              resource.
                            enum: [transaction, batch, boarding, paymentUpdates, payout,
                              payoutCredit, payoutDebit, chargeback, txnReport, account,
                              entityDecision, txnCheck, payoutReport, feeReport, chargebackReport,
                              binReport, watchlist, encryption, changeRequest]
                            x-enum-descriptions: [Credential used for transaction processing,
                              Credential used for transaction batches, Credential used
                                merchant boarding, '', Credential used for payouts,
                              '', '', Credential used for chargeback processing and
                                updating, Credential used for transaction reports, Credential
                                used for account integrations, Credential used for entity
                                decisions, Credential used for transaction verifications,
                              Credential used for payout reports, Credential used for
                                fee reports, Credential used for chargeback reports,
                              Credential used binReports, '', '', '']
                          url:
                            description: |-
                              The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                              Valid placeholders are: entity, merchant, member, chargeback, and document.
                              The field is stored as a text string and must be between 1 and 500 characters long
                          dir:
                            description: "The directory used once connected. \nThis\
                              \ field is stored as a text string and must be between\
                              \ 1 and 500 characters long"
                          restId:
                            description: "The indicator of the field when ref Id is\
                              \ found. \nThis field is stored as a text string and must\
                              \ be between 0 and 100 characters long."
                          mapToData: {}
                          file:
                            description: "The specification on how the file will be\
                              \ named on write. \nThis field is stored as a JSON string\
                              \ and must be between 0 and 5000 characters long."
                          filter:
                            description: "The filter applied when successful on data\
                              \ read. \nThis field is stored as a JSON string and must\
                              \ be between 0 and 5000 characters long."
                          extension:
                            description: The extension of the file.
                            enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc,
                              rtf, csv, xls, doc, odt, ods, json, soap]
                            x-enum-descriptions: [jpg extension file type for the input
                                and output files, jpeg extension file type for the input
                                and output files, gif extension file type for the input
                                and output files, png extension file type for the input
                                and output files, pdf extension file type for the input
                                and output files, tif extension file type for the input
                                and output files, tiff extension file type for the input
                                and output files, txt extension file type for the input
                                and output files, xml extension file type for the input
                                and output files, asc extension file type for the input
                                and output files, rtf extension file type for the input
                                and output files, csv extension file type for the input
                                and output files, xls extension file type for the input
                                and output files, doc extension file type for the input
                                and output files, odt extension file type for the input
                                and output files, ods extension file type for the input
                                and output files, '', '']
                          namespace:
                            description: "The namespace used for the configurationStage\
                              \ resource. \nThis field is stored as a text string and\
                              \ must be between 0 and 500 characters long."
                          dynamicStage:
                            description: "Only used in case the it is dynamic stage.\
                              \ \nThis field is stored as a JSON string, must be between\
                              \ 0 and 5000 characters long, and have a property key\
                              \ name with value of stage name and id key."
                          curlOptions: {}
                          headers:
                            description: "The headers used for the configurationStage\
                              \ resource. \nThis field is stored as a JSON string and\
                              \ must be between 0 and 5000 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
          application/xml:
            schema:
              properties:
                response:
                  properties:
                    data:
                      description: The data object that the API returns for this request.
                      items:
                        properties:
                          id: {description: The ID of this resource.}
                          created: {description: The date and time at which this resource
                              was created., format: date-time}
                          modified: {description: The date and time at which this resource
                              was modified., format: date-time}
                          creator: {description: The identifier of the Login that created
                              this resource.}
                          modifier: {description: The identifier of the Login that last
                              modified this resource.}
                          configuration: {description: The identifier of the Configuration
                              that owns this configurationStage resource.}
                          name:
                            description: The name of this configurationStage resource.
                            enum: [blank, acceptedCards, ack, activateSrs, alert, amexUpdate,
                              applepay, balance, batch, chargeback, chargebackList,
                              confirmDisbursement, contactUpdate, createMerchant, credit,
                              decrypt, debit, debitPinEnable, debitPinlessEnable, discoverUrl,
                              discoverUpdate, document, documentList, interchange, encrypt,
                              entity, entityAddressUpdate, entityRetrieval, entityUpdate,
                              expressSubAccountUpdate, return, feeList, googlepay, memberCreate,
                              memberDelete, members, memberUpdate, merchant, merchantCardPresent,
                              merchantCardNotPresent, merchantUpdate, merchantRetrieval,
                              noc, nocReturns, omniTokenEnable, passTokenEnable, payment,
                              paymentConfirmation, paymentUpdate, payout, primaryMemberUpdate,
                              registerSrs, returnCredit, returnDebit, returns, saferPaymentEnable,
                              terminalCreate, terminalList, terminalsSupported, txn,
                              txnList, varsheet]
                            x-enum-descriptions: [Blank. Used for a stage with no name,
                              '', '', '', '', '', '', '', '', Chargeback. The standard
                                name used for chargeback related stages, ChargebackList.
                                The specific name to get chargeback reports, '', '',
                              '', '', '', '', '', '', '', '', 'Document. The standard
                                name for chargeback related documentation such as uploading
                                a document or updating a document ', DocumentList. Used
                                for receiving documents related to a chargeback, '',
                              '', Entity. The standard name used when it comes to entity
                                related stages such as retrieving an entity or updating,
                              '', EntityRetrieval. Used when entity stage name is already
                                being used in that crud configuration, EntityUpdate.
                                Used when entity stage name is already being used in
                                that crud configuration, '', Return. Used for return
                                stage name, FeeList. The specific name to get fees reports,
                              '', MemberCreate. Stage name used to indicate creating
                                a merchant member, MemberDelete. Stage name used to
                                indicate deleting a merchant member, Members. Stage
                                name used for creating several members for the merchant,
                              '', Merchant. The standard name used for merchant related
                                stages such retrieving a merchant or updating a merchant,
                              '', '', '', MerchantRetrieval. Used when merchant stage
                                name is already being used in that crud configuration,
                              Noc. Stage name used for notification of change for disbursement
                                related integrations, '', '', '', '', '', '', Payout.
                                Stage name used for payouts, '', '', '', '', '', '',
                              '', '', '', Txn. The standard name used for transaction
                                related stages, TxnList. The specific name to get transaction
                                reports, '']
                          method:
                            description: The method used for configurationStage resource.
                            enum: [sftpUpload, sftpDownload, post, get, put, delete,
                              tcp]
                            x-enum-descriptions: [SFtpUpload. Will use sftp to upload,
                              '', Post. Will use a post request, Get. Will use a get
                                request, Put. Will use a put request, Delete. Will use
                                a delete request, Tcp. Will use tcp protocol]
                          sequence: {}
                          input:
                            description: The input used for configurationStage resource.
                            enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                              APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT,
                              ELAVON_BATCH_READ_FLAT, ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV,
                              ELAVON_TXN_XML, FEDACH_REPORT_FLAT, FIRSTDATA_BATCH_FLAT,
                              FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON, FIRSTDATA_REPORTS_FLAT,
                              FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                              FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM,
                              NEUTRINO_JSON, OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON,
                              PAYRIX_ENCRYPTION_JSON, PAYRIX_GOOGLEPAY_DECRYPTION_JSON,
                              PAYRIX_TXN_JSON, PUSHY_TXN_JSON, SIFT_JSON, SOCURE_ALLOY_JSON,
                              SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM, SOUNDPAYMENTS_TXN_XML,
                              VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                              VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                              VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV,
                              VANTIV_TXN_XML, VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT,
                              VCORE_BINREPORT_FLAT, VCORE_BOARD_API, VCORE_BOARD_FLAT,
                              VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                              VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT,
                              VCORE_VARSHEET_JSON, WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT,
                              WELLSACH_PAYOUT_FLAT, WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML,
                              WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT, WFSINGLE_REPORT_FLAT,
                              WFSINGLE_BOARD_XML]
                            x-enum-descriptions: ['', '', '', '', '', '', '', '', '',
                              '', '', '', '', '', '', '', '', '', NACHA input file,
                              '', NEUTRINO_FORM input file, '', OFAC_XML input file,
                              '', '', '', '', '', '', SIFT_JSON input file, '', SOCURE_FORM
                                input file, SOCURE_JSON input file, SOUNDPAYMENTS_TXN_FORM
                                input file, SOUNDPAYMENTS_TXN_XML input file, VANTIV_BATCH_XML
                                input file, VANTIV_BOARD_XML input file, VANTIV_CHARGEBACK_XML
                                input file, '', VANTIV_PAYOUT_CSV input file, VANTIV_PAYOUT_XML
                                input file, '', VANTIV_REPORTS_CSV input file, VANTIV_TXN_CSV
                                input file, VANTIV_TXN_XML input file, VCORE_BATCH_FLAT
                                input file, VCORE_BATCH_READ_FLAT input file, '', '',
                              VCORE_BOARD_FLAT input file, VCORE_CHARGEBACK_FLAT input
                                file, '', VCORE_REPORTS_FLAT input file, '', VCORE_TXN_ISO8583
                                input file, VCORE_UPDATE_FLAT input file, '', WEBSHIELD_FORM
                                input file, WEBSHIELD_JSON input file, '', '', '', '',
                              '', '', '', '']
                          output:
                            description: The output used for configurationStage resource.
                            enum: [APPLE_PAYMENTSESSION_REQUEST_JSON, APPLE_REGISTER_MERCHANT_REQUEST_JSON,
                              APPLE_UNREGISTER_MERCHANT_REQUEST_JSON, EFT, ELAVON_BATCH_FLAT,
                              ELAVON_BATCH_READ_FLAT, ELAVON_BOARD_JSON, ELAVON_REPORTS_CSV,
                              ELAVON_TXN_XML, FEDACH_REPORT_FLAT, FIRSTDATA_BATCH_FLAT,
                              FIRSTDATA_BATCH_READ_FLAT, FIRSTDATA_BOARD_JSON, FIRSTDATA_REPORTS_FLAT,
                              FIRSTDATA_SRS_XML, FIRSTDATA_TXN_XML, FIRSTDATA_SINGLE_BATCH_FLAT,
                              FIRSTDATA_SINGLE_TXN_XML, NACHA, NEUTRINO_CSV, NEUTRINO_FORM,
                              NEUTRINO_JSON, OFAC_XML, PAYRIX_CHARGEBACK_JSON, PAYRIX_DECISION_JSON,
                              PAYRIX_ENCRYPTION_JSON, PAYRIX_GOOGLEPAY_DECRYPTION_JSON,
                              PAYRIX_TXN_JSON, PUSHY_TXN_JSON, SIFT_JSON, SOCURE_ALLOY_JSON,
                              SOCURE_FORM, SOCURE_JSON, SOUNDPAYMENTS_TXN_FORM, SOUNDPAYMENTS_TXN_XML,
                              VANTIV_BATCH_XML, VANTIV_BOARD_XML, VANTIV_CHARGEBACK_XML,
                              VANTIV_PAYMENT_UPDATES_XML, VANTIV_PAYOUT_CSV, VANTIV_PAYOUT_XML,
                              VANTIV_PAYOUT_XML_V9, VANTIV_REPORTS_CSV, VANTIV_TXN_CSV,
                              VANTIV_TXN_XML, VCORE_BATCH_FLAT, VCORE_BATCH_READ_FLAT,
                              VCORE_BINREPORT_FLAT, VCORE_BOARD_API, VCORE_BOARD_FLAT,
                              VCORE_CHARGEBACK_FLAT, VCORE_OMNITOKEN_JSON, VCORE_REPORTS_FLAT,
                              VCORE_SAFERPAYMENT_JSON, VCORE_TXN_ISO8583, VCORE_UPDATE_FLAT,
                              VCORE_VARSHEET_JSON, WEBSHIELD_FORM, WEBSHIELD_JSON, WELLSACH_BATCH_FLAT,
                              WELLSACH_PAYOUT_FLAT, WELLSACH_TXN_FLAT, WELLSFARGO_BOARD_XML,
                              WELLSFARGO_REPORT_CSV, WELLSFARGO_REPORT_FLAT, WFSINGLE_REPORT_FLAT,
                              WFSINGLE_BOARD_XML]
                            x-enum-descriptions: ['', '', '', '', '', '', '', '', '',
                              '', '', '', '', '', '', '', '', '', NACHA output file,
                              '', NEUTRINO_FORM output file, '', OFAC_XML output file,
                              '', '', '', '', '', '', SIFT_JSON output file, '', SOCURE_FORM
                                output file, SOCURE_JSON output file, SOUNDPAYMENTS_TXN_FORM
                                output file, SOUNDPAYMENTS_TXN_XML output file, VANTIV_BATCH_XML
                                output file, VANTIV_BOARD_XML output file, VANTIV_CHARGEBACK_XML
                                output file, '', VANTIV_PAYOUT_CSV output file, VANTIV_PAYOUT_XML
                                output file, '', VANTIV_REPORTS_CSV output file, VANTIV_TXN_CSV
                                output file, VANTIV_TXN_XML output file, VCORE_BATCH_FLAT
                                output file, VCORE_BATCH_READ_FLAT output file, '',
                              '', VCORE_BOARD_FLAT output file, VCORE_CHARGEBACK_FLAT
                                output file, '', VCORE_REPORTS_FLAT output file, '',
                              VCORE_TXN_ISO8583 output file, VCORE_UPDATE_FLAT output
                                file, '', WEBSHIELD_FORM output file, WEBSHIELD_JSON
                                output file, '', '', '', '', '', '', '', '']
                          credential:
                            description: The credential used for configurationStage
                              resource.
                            enum: [transaction, batch, boarding, paymentUpdates, payout,
                              payoutCredit, payoutDebit, chargeback, txnReport, account,
                              entityDecision, txnCheck, payoutReport, feeReport, chargebackReport,
                              binReport, watchlist, encryption, changeRequest]
                            x-enum-descriptions: [Credential used for transaction processing,
                              Credential used for transaction batches, Credential used
                                merchant boarding, '', Credential used for payouts,
                              '', '', Credential used for chargeback processing and
                                updating, Credential used for transaction reports, Credential
                                used for account integrations, Credential used for entity
                                decisions, Credential used for transaction verifications,
                              Credential used for payout reports, Credential used for
                                fee reports, Credential used for chargeback reports,
                              Credential used binReports, '', '', '']
                          url:
                            description: |-
                              The url used for configurationStage resource. It will consist of a combination of at least one path, and restful id. Placeholders can be used for the restful ids such as https://sample.com/:placeholder:.
                              Valid placeholders are: entity, merchant, member, chargeback, and document.
                              The field is stored as a text string and must be between 1 and 500 characters long
                          dir:
                            description: "The directory used once connected. \nThis\
                              \ field is stored as a text string and must be between\
                              \ 1 and 500 characters long"
                          restId:
                            description: "The indicator of the field when ref Id is\
                              \ found. \nThis field is stored as a text string and must\
                              \ be between 0 and 100 characters long."
                          mapToData: {}
                          file:
                            description: "The specification on how the file will be\
                              \ named on write. \nThis field is stored as a JSON string\
                              \ and must be between 0 and 5000 characters long."
                          filter:
                            description: "The filter applied when successful on data\
                              \ read. \nThis field is stored as a JSON string and must\
                              \ be between 0 and 5000 characters long."
                          extension:
                            description: The extension of the file.
                            enum: [jpg, jpeg, gif, png, pdf, tif, tiff, txt, xml, asc,
                              rtf, csv, xls, doc, odt, ods, json, soap]
                            x-enum-descriptions: [jpg extension file type for the input
                                and output files, jpeg extension file type for the input
                                and output files, gif extension file type for the input
                                and output files, png extension file type for the input
                                and output files, pdf extension file type for the input
                                and output files, tif extension file type for the input
                                and output files, tiff extension file type for the input
                                and output files, txt extension file type for the input
                                and output files, xml extension file type for the input
                                and output files, asc extension file type for the input
                                and output files, rtf extension file type for the input
                                and output files, csv extension file type for the input
                                and output files, xls extension file type for the input
                                and output files, doc extension file type for the input
                                and output files, odt extension file type for the input
                                and output files, ods extension file type for the input
                                and output files, '', '']
                          namespace:
                            description: "The namespace used for the configurationStage\
                              \ resource. \nThis field is stored as a text string and\
                              \ must be between 0 and 500 characters long."
                          dynamicStage:
                            description: "Only used in case the it is dynamic stage.\
                              \ \nThis field is stored as a JSON string, must be between\
                              \ 0 and 5000 characters long, and have a property key\
                              \ name with value of stage name and id key."
                          curlOptions: {}
                          headers:
                            description: "The headers used for the configurationStage\
                              \ resource. \nThis field is stored as a JSON string and\
                              \ must be between 0 and 5000 characters long."
                          inactive:
                            description: Whether this resource is marked as inactive.
                            enum: ['0', '1']
                            x-enum-descriptions: [Active, Inactive]
                          frozen:
                            description: Whether this resource is marked as frozen.
                            enum: ['0', '1']
                            x-enum-descriptions: [Not frozen, Frozen]
                    details:
                      description: An object describing the response, including the
                        request ID and pagination indicators.
                      properties:
                        requestId:
                          description: "A unique identifier set by the API client, that\
                            \ is echoed by the response. \nThe request ID can be useful\
                            \ in troubleshooting and monitoring."
                        page:
                          description: "Where the response lists multiple resources,\
                            \ the API splits the response into several 'pages'. \nThis\
                            \ object indicates the current and last available pages\
                            \ in the list."
                          properties:
                            current: {description: The current page in the paginated
                                resource list.}
                            last: {description: The last available page in the paginated
                                resource list.}
                    errors:
                      description: An array of zero or more errors that occurred when
                        the API processed the request.
                      items:
                        properties:
                          code: {description: The error code associated with a particular
                              error.}
                          severity:
                            description: "The severity level of a particular error.\
                              \ \nPossible values are '0' (informational), '1' (warning),\
                              \ '2' (error) and '4' (failure)."
                          message: {description: The message associated with a particular
                              error.}
                          errorCode: {description: An identifying string code for this
                              error.}
        description: Response for create on configurationStages resource
    tags: [Configurations]
    operationId: postConfigurationStages
