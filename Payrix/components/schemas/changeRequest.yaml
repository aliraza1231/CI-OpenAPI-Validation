properties:
  deleted: {type: string, description: 'In case resource is deleted, this field will then show the date it occurred', format: date-time}
  login: {type: string, description: The identifier of the Login associated with this
            ChangeRequest.}
  authType: {type: string, description: Auth Type that originate the request.}
  status:
    type: string
    description: The status of the requested change.
    enum: [pending, manualReview, approved, declined]
    x-enum-descriptions: [Pending. The Change Request has a pending status.,
                          ManualReview. The Change Request was escalated to be manually reviewed.,
                          Approved. The Change Request has been approved., '']
  reasonType:
    type: string
    description: A type which categorizes the reason.
    x-enum-descriptions: ['']
  reason: {type: string, description: The reason in case the change was declined.}
  recordId: {type: string, description: 'The id of the model, only set when operation
                 is update'}
  analyst: {type: string, description: The person who review the request.}
  reviewed: {type: string, description: 'If the change request was reviewed, this field will then show the date it occurred', format: date-time}
required: [login, reason, reasonType, status]
x-redacted: []