properties:
  fee: { type: string, description: The identifier of the Fee that this Fee Modifier
           applies. }
  entity: { type: string, description: The identifier of the Entity that this Fee
    Modifier applies for. }
  org: { type: string,
         description: "The identifier of the Org this Fee Modifiers should\
                          \ apply for on behalf of the Entity identified in the value\
                          \ of the 'entity' field. \nThis field is optional. If it is\
                          \ set, then the Fee Modifier is applied to this Org instead." }
  fromentity: { type: string,
                description: "The identifier of the Entity who should pay this\
                          \ Fee on behalf of the Entity identified in the value of the\
                          \ 'entity' or 'org' field. \nThis field is optional. If it\
                          \ is set, then the Fee is charged to this Entity instead." }
  markupUm: { type: integer,
              description: "The unit of measure for the markup amount for the Fee.\n\n
              <details><summary>Valid Values</summary>\n\
              \n - `1` - **Fixed Amount.** Specified in the 'markupAmount' field as an integer in cents.\n\
              \n - `2` - **Percentage.** Specified in the 'markupAmount' field in basis points.\n\
              \n - `3` - **''**\n\n</details>\n\n",
              enum: [ 1, 2, 3 ] }
  markupAmount: { type: integer,
                  description: "The total amount of the markup value for this\
                          \ Fee. \nThe units used in this field are determined by the\
                          \ value of the 'markupUm' field on the Fee. If the 'markupUm'\
                          \ field is set to 'percentage', then this field specifies\
                          \ the Fee percentage to levy in basis points. If the 'markupUm'\
                          \ field is set to 'actual', then this field specifies the\
                          \ markup amount in cents." }
  inactive: { $ref: ../inactive.yaml }
  frozen: { $ref: ../frozen.yaml }
required: [ fee, frozen, inactive ]
x-redacted: [ ]