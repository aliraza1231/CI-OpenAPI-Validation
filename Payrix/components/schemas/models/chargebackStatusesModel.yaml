properties:
  id: { $ref: ../id.yaml }
  created: { $ref: ../created.yaml }
  modified: { $ref: ../modified.yaml }
  creator: { $ref: ../creator.yaml }
  modifier: { $ref: ../modifier.yaml }
  chargeback: { type: string, description: "ID of the related chargeback." }
  chargebackMessage: { type: string, description: "ID of the chargeback message associated with this chargeback." }
  status: { type: string, description: "The Chargeback's status.
                  <details><summary>Valid Values</summary>\n\
                  \n - `open` - **Chargeback is open, responses may be submitted.**\n\
                  \n - `closed` - **Chargeback is closed, responses may no longer be submitted.**\n\
                  \n - `won` - **Chargeback won.**
                  \n - `lost` - **Chargeback lost.**\n\n
                  </details>",
            enum: [ open, closed, won, lost ] }