properties:
  ref: { type: string, description: The reference code itself. This field is
    stored as a text string and must be between 0 and 50 characters
    long. }
  processor: { type: string,     description: "The platform used for the adjustment. This
    field is required if the adjustment is not between entities.",
               enum: [ APPLE, ELAVON, FIRSTDATA, GOOGLE, VANTIV, VCORE,
                       WELLSACH, WELLSFARGO, WFSINGLE, WORLDPAY, TDBANKCA ],
               example: VCORE,
               x-enum-descriptions: [ '', '', '', '', '', '', '', '', '',
                                      '', '' ] }
required: [ processor, ref ]
x-redacted: [ ]