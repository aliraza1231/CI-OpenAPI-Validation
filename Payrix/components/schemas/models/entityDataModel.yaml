entity: { type: "string", description: The Entity associated with this EntityData. }
tcAcceptSignature: { type: "string", description: The Base64 encoded image of the signature captured at the time of creating the Entity.}